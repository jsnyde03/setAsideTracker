# v1.1 — Comprehensive Pre-Submit TestFlight Checklist

> ## ✅ FULL PASS — 2026-07-01 · v1.1 LOCKED (tag `v1.1.0-rc1`)
> Every section (0, 0.1, A–I) passed on a real device / locally / on CI. The pre-submit device-QA gate
> is **cleared** — v1.1 is **submittable**. One notable catch, resolved: an alarming $7,200 W2 set-aside
> traced to *guessed* profile inputs (user error, not a calc bug — see 0.1). **v1.1 stays parked on its
> branch; do NOT merge to `master` until v1.0 is live.** When v1.0 clears review → merge → submit v1.1
> (with the ASO metadata) → flip Premium live.

> **This is the go-live gate for v1.1.** Per the hard rule ([memory: pre-submit TestFlight QA gate]),
> v1.1 is **not submittable for App Store review until this passes top-to-bottom on a real device.**
> It **supersedes** [PHASE_B_TESTFLIGHT_CHECKLIST.md](PHASE_B_TESTFLIGHT_CHECKLIST.md) for the
> full-version pass — that one validated only the Phase B *exit subset* (IAP + PDF). This covers the
> **entire v1.1 surface**, and especially the **~11 features that landed *after* that pass and have
> never run on a device**: mileage-log fields, custom expense categories, W-4 optimizer, safe-harbor,
> year-over-year, and the four PDF/on-screen follow-ons.

**Why device-only:** off-device is already green (tsc, 144 unit tests, 17-test Playwright, Maestro
flows authored). None of that exercises the **native paths** that break in the real world — the
priority of this pass. Run it as **one build** to respect the Codemagic budget; don't ship a second
build to catch a missed step. Check the box only when verified on the device.

---

## 0. Before the build  ✅ PASSED 2026-06-30

- [x] **Sandbox tester** exists (App Store Connect → Users and Access → Sandbox → Testers) and you're
  signed into it on the device (Settings → App Store → Sandbox Account) — required to buy without a charge.
- [x] **IAP review screenshot** attached to both products in ASC (needed for Apple to *approve* the
  IAP, not for testing) — ask Claude to generate a paywall screenshot if not done.
- [x] Build is the **latest `v1.1`** (push first) at **version 1.1.0**; installed fresh via TestFlight.
- [x] Test device is a real iPhone (not just the simulator) — Face ID/notifications/share/IAP are the point.

## 0.1 Seed a realistic persona (so premium screens are populated)

- [x] Profile: real-sounding name; **a state WITH income tax** (e.g. NY/CA) so federal **and** state lines show. _(MD / Garrett County — state + local.)_
- [x] Turn **"I also have a W2 job" ON** with believable pay-stub numbers (required for the W-4 shot in F5).
  **✅ Verified 2026-06-30:** clean $65k W2 ($2,500 biweekly), MD / Garrett County → set-aside **$0**, both
  with blank optional fields and with realistic 401k/pretax + mid-year YTD withholding. Confirms the W2
  withholding credit (incl. **MD local county tax** and the **YTD hybrid path**) correctly zeroes the
  set-aside when there's no gig tax to cover. An earlier alarming **$7,200** traced to *guessed* W2
  inputs — **user error, not a calc bug** (the "≈ $/yr gross" hint is the guardrail against it).
- [x] Log **6–8 entries** across 3–4 platforms, over the last couple of weeks, believable amounts; several
  with mileage + expenses; at least one with hours (so effective-hourly-rate shows).
- [x] On a couple of trips, fill the **mileage-log** purpose/start/end and add a **custom expense** or two.
- [x] Set "amount set aside so far" into the **green "on track"** band.

---

## A. Free core smoke (the v1.0 base, re-verified in the merged build)  ✅ PASSED 2026-06-30

- [x] Onboarding / profile form completes; dashboard renders with the **set-aside gradient card** (SE + federal + state lines).
- [x] **Log Earnings** — the **native date picker** opens, sets a date, and the entry saves; the set-aside number updates immediately.
- [x] **Edit** an existing entry and **delete** one (native destructive confirm) — totals recompute correctly.
- [x] **Year switcher** changes the active tax year; numbers scope to the selected year.
- [x] **Dark mode** (Settings → Appearance) — all core screens readable, no clipped/again-white cards.
- [x] Rotate / large Dynamic Type sanity (no critical clipping on the numeric screens).

## B. Free Phase A features  ✅ PASSED 2026-06-30

- [x] **Show your math** — tap a breakdown line (federal / SE / state); the sheet opens and the math reconciles (AGI − std deduction = taxable; brackets sum to the row).
- [x] **Tax-literacy glossary** — the "what these mean" pills expand plain-language definitions inline.
- [x] **What-if simulator** — change hypothetical earnings; set-aside + effective hourly rate recompute live.
- [x] **Platform comparison** — per-platform earnings + after-expense hourly rate render for the seeded platforms.
- [x] **Share card (NATIVE — view-shot + share sheet):** tap share → the branded image renders and the **iOS share sheet** opens; AirDrop/Save-to-Photos produces a real image (this is the `react-native-view-shot` path Playwright can't test).
- [x] **Rating prompt (NATIVE — StoreReview):** after the 5th entry / first catch-up, the OS rating dialog appears **once** and doesn't re-nag (may be rate-limited by iOS in sandbox — confirm it at least doesn't crash).

## C. Native trust / privacy  ✅ PASSED 2026-06-30

- [x] **Biometric app lock** — enable in Settings; background + reopen → **Face ID / Touch ID** challenge gates entry; a failed/cancelled auth keeps data locked.
- [x] **Quarterly reminder notification** — the app schedules the estimated-tax due-date reminder; the OS permission prompt appears and a scheduled local notification is registered (verify via the permission-granted + iOS-Settings "Allow Notifications" quick check; optional clock fast-forward for delivery proof).
- [x] **CSV export (native share)** — exports a CSV via the share sheet; open it and confirm columns incl. the **mileage-log** fields and the **custom-expenses** column.
- [x] **Backup → Restore round-trip** — create a backup file, wipe/reinstall (or restore over), and confirm entries + profile come back intact (native file read/write).

## D. Premium gating from the FREE state (each entry point routes to the paywall)  ✅ PASSED 2026-06-30

_Before purchasing, confirm every gated surface locks correctly and routes to the paywall — several are
native "locked row → Alert → paywall" hops only Maestro exercises off-device:_

- [x] Settings **"Tax Summary (PDF) · Premium"** → paywall (does not export).
- [x] Add-Entry **"IRS mileage log"** locked Premium row → Alert → paywall; returns to the entry form on dismiss.
- [x] Add-Entry **"Custom expense categories"** locked Premium row → Alert → paywall.
- [x] Dashboard **"Expense breakdown · Premium"** card (shown when a deductible expense/mileage exists) → straight to paywall.
- [x] Dashboard **"Skip quarterly payments · Premium"** (W-4) card (shown only with the W2 job on) → paywall.
- [x] Dashboard **"Avoid the IRS penalty · Premium"** (safe-harbor) card → paywall.
- [x] Dashboard **"Year-over-year insights · Premium"** card (shown once entries span 2+ years — see F7) → paywall.
- [x] Paywall itself (Apple 3.1.2): both **RevenueCat prices** ($29.99/yr, $4.99/mo), auto-renewal disclosure, Terms/Privacy links, Subscribe, Restore — price is the most prominent element.

## E. Purchase → unlock → restore → offline  ✅ PASSED 2026-07-01

- [x] **Subscribe (annual)** → native sandbox purchase sheet → confirm → paywall dismisses; app reflects **premium active** (locks gone, "· Premium" suffixes drop).
- [x] Analytics funnel fired: `paywall_viewed → purchase_started → purchase_completed` (confirm in PostHog if convenient).
- [x] **Restore** — delete + reinstall (or sign out/in) → **Restore purchases** → premium returns.
- [x] **Offline entitlement** — airplane mode with an active sub → premium **stays unlocked** and exports still work.

## F. Premium features (now unlocked)  ✅ PASSED 2026-07-01

**F1 — PDF tax-ready export (NATIVE print → share):**
- [x] With ≥1 entry this year, export → PDF renders, **system share sheet** opens, file named `tax-summary-<year>.pdf`.
- [x] **Schedule C** numbers reconcile: Line 1 gross = earnings + tips; **Line 9** = mileage (miles × rate, per the note) + parking + tolls; Line 22 supplies; Line 25 phone; **Line 27 "Other expenses"** shows the **per-custom-category** sub-rows; Line 31 net = gross − expenses.
- [x] **Mileage-log appendix** ("Schedule C Line 9 Substantiation") present: one row per business-mile trip (date · purpose · route · miles), oldest-first, total reconciles to Line 9; blank purpose falls back to the platform label, blank route to a dash; **no zero-mile rows**.
- [x] **Safe-harbor section** present: both legs (90% current vs 100/110% prior) with the **binding leg marked**, required annual payment, W-2 withholding credited (row omitted if 0), estimated payments, **≈ per-quarter 1040-ES** figure — OR a plain no-penalty note when under the $1,000 floor.
- [x] "Estimated amount to set aside" in the PDF **matches the dashboard**. Header (name, filing status, state/county, date) + disclaimer present.
- [x] **Empty-year guard:** with no entries this year, export shows the "log at least one entry" alert, not an empty PDF.

- [x] **F2 — Mileage-log fields:** the purpose/start/end entered in 0.1 appear in the **CSV** (C) and the **PDF appendix** (F1).
- [x] **F3 — Custom expense categories:** the custom lines reduce **net SE profit** in the estimate, land on **Line 27** in the PDF, and show a **CSV** column.
- [x] **F4 — On-screen expense breakdown:** open the screen → Line 9 composition (mileage + parking/tolls), 22, 25, 27 with per-custom-category sub-rows, Line 28 total, and net profit — all matching the PDF; empty/hint states behave.
- [x] **F5 — W-4 optimizer (needs the W2 job on):** result screen shows the extra per-paycheck **Line 4(c)** withholding + a date-aware catch-up figure; sanity-check it's roughly (gig tax ÷ remaining paychecks).
- [x] **F6 — Safe-harbor / Form 2210:** enter last year's **filed federal tax** (prior-year input; a suggestion may prefill) → the prior-year leg binds when income jumped; the required amount and per-quarter figure are sensible; MFS/110% threshold behaves.
- [x] **F7 — Year-over-year (NATIVE date picker to seed a 2nd year):** log an entry dated **in the prior tax year** (drive the native picker back a year) so entries span 2 years → the dashboard YoY card appears → the screen shows the latest-vs-previous deltas (earnings/profit/hourly-rate colored by direction; expenses/miles/tax neutral), the all-years table, and — if a filed figure was entered in F6 — the **"Filed federal tax on record"** card (separate from the combined estimate, with its footnote). With only one year, it shows the friendly "come back next year" state instead.

## G. Financial-accuracy spot-checks (cross-cutting)  ✅ PASSED 2026-07-01

- [x] The **set-aside number** is consistent everywhere it appears (dashboard, PDF, what-if base case).
- [x] Changing filing status / state in the profile moves the numbers in the expected direction.
- [x] A **zero-income / no-tax-owed** state renders sanely (no negative set-aside, no NaN) — the boundary case.

## H. Stability & observability  ✅ PASSED 2026-07-01

- [x] No crashes across the full run; background/foreground and rotate without state loss.
- [x] **Sentry** receives events (confirm the build reports in; optionally trigger a non-fatal to verify symbolication).
- [x] **PostHog** shows the session + the premium-funnel events from E.

## I. Release / regression gate (off-device, confirm green before submitting)

- [x] `tsc` clean · **144 mobile unit tests** green (+ **101 engine tests**, engine build clean) — **verified locally 2026-07-01** on `v1.1`.
- [x] **17-test Playwright** e2e — ran green on CI (`web-e2e`) from the push. ✅ confirmed in Codemagic 2026-07-01.
- [x] **Maestro** flows green on the Codemagic mac run (`maestro-ios`, the native gating hops). ✅ confirmed in Codemagic 2026-07-01.
- [x] Production/Release build succeeds — proven by the v1.1 TestFlight build (it installed + ran on device).

---

## Pass = submittable

When **A–I all pass on the device**, the pre-submit go-live gate is cleared and v1.1 is **submittable**.
The remaining gate is external: **v1.0 clearing App Store review** → then merge `v1.1` → `master`,
submit v1.1 for review (ASO metadata from `STORE_LISTING.md` + `SCREENSHOT_PLAN.md`), and flip Premium
live. Record the pass date at the top of this file (like the Phase B checklist) when done.
