# Gig Tax Tracker — Implementation Plan by Version

Companion to [ROADMAP.md](ROADMAP.md). The roadmap covers *what* and *why*; this breaks it into shippable versions — each one is a real, usable milestone (internal, beta, or public), not just a sprint boundary. Versions follow `major.minor`: major bumps = new monetization tier or major capability shift (AI layer, etc.); minor bumps = a coherent batch of features shippable on its own.

**Status legend:** ✅ Done · 🔄 In Progress · ⬜ Not Started

## ✅ v0.1 — Tax Engine Proof of Concept (internal only)

<details>
<summary>Status: Done — services/tax-engine/</summary>

**Status: Done** — `services/tax-engine/`
**Goal:** prove the core math is right before building anything else on top of it.
- [x] Standalone tax-engine package: SE tax, federal bracket calc, standard mileage deduction
- [x] Versioned tax-year config — shipped with **2025** rates initially, then **2026** added once official IRS Rev. Proc. 2025-32 (brackets/standard deduction), Notice 2026-10 (mileage rate: $0.725/mi), and the SSA 2026 COLA fact sheet (SS wage base: $184,500) were confirmed. `currentTaxYear` now points at the 2026 config; `taxYear2025` stays available for historical accuracy. per [ROADMAP §2.2](ROADMAP.md)
- [x] Unit tests against hand-verified IRS-style scenarios for **both** 2025 and 2026 configs (single filer, multiple income levels including SS wage base cap + Additional Medicare Tax threshold) — 18 tests passing
- [x] No UI — test harness only, via Vitest
**Ships to:** you/dev team only. **Exit criteria:** ✅ met — tax engine outputs match hand-calculated IRS examples exactly for both tax years.

</details>

## ✅ v0.2 — Manual Entry + Dashboard (internal alpha)

<details>
<summary>Status: Done — apps/mobile/ (Expo/React Native + TypeScript)</summary>

**Status: Done** — `apps/mobile/` (Expo/React Native + TypeScript)
**Goal:** the core daily-use loop works end to end on a device.
- [x] Auth — **stubbed as a local profile** (name/email, no password, stored on-device via AsyncStorage). Real Apple/Google OAuth deferred until Apple Developer/Google Cloud credentials are available — by design, see decision log below.
- [x] Manual entry flow: platform (Amazon Flex/Spark/DoorDash/Uber/Instacart/Other), date, gross pay, tips, mileage
- [x] Single dashboard: total earnings, estimated tax owed ("set aside for taxes"), entry list
- [x] Tax profile onboarding wizard (filing status, dependents, W2 job y/n + estimated W2 income, state)
- [x] All data local-only (AsyncStorage) — no backend yet, per decision log below
- [x] Calculation glue layer (`src/calculations.ts`) aggregates entries and calls the tax-engine package directly as a workspace dependency
- [x] Unit tests for the aggregation/engine-wiring logic (5 tests) + full type-check clean
- [x] **Browser dev mode confirmed working** — `npm run web` (`expo start --web`) boots Metro, serves the app at `localhost:8081`, and the full bundle compiles clean (verified by fetching the actual bundle and dev server response). Added `react-dom` + `react-native-web` for this.
**Ships to:** internal device testing / friends & family (via Expo Go on a phone, or browser dev mode for fast iteration — see below). **Exit criteria:** core loop (onboard → log entry → see dashboard update) works end to end; verified via web bundle compile, not yet click-tested in an actual browser window or on a physical device — that's the next concrete step.

**Decision log (confirmed with you before building):**
- Auth: local stub now, real OAuth later once credentials exist
- Backend: local-only for this alpha; server-side sync is in scope for v0.3's offline-first work, not before

**Dev workflow note:** run `npm run web` from `apps/mobile/` for fast iteration in a browser — no TestFlight/device build needed for day-to-day UI changes. Reserve actual device builds (Expo Go, or eventually TestFlight) for verifying things browser dev mode can't catch: native-only APIs, real touch/gesture behavior, and final pre-release sign-off. AsyncStorage works in the browser too (backed by IndexedDB), so local data persistence behaves the same way there.

**Bug fixed during verification:** browser mode initially loaded a blank white page with no visible error. Root cause was a `react`/`react-dom` version mismatch (19.2.3 vs 19.2.0) — React 19 fails hard on any mismatch and React Native Web's error overlay doesn't always surface it clearly. Found it by loading the page in a headless browser and reading the actual console/page error rather than guessing. Fixed by pinning `react-dom` to the exact same version as `react` (`"19.2.3"`, no `^` range) so they can't drift apart again on a future `npm install`.

</details>

## ✅ v0.3 — Closed Beta (expenses + reminders)

<details>
<summary>Status: Done — all planned items shipped; see exit-criteria caveat below before calling t…</summary>

**Status: Done** — all planned items shipped; see exit-criteria caveat below before calling this fully verified.
**Goal:** feature-complete enough for a small group of real gig workers to rely on.
- [x] **State tax module** for 6 states — `services/tax-engine/src/stateTax.ts` + `src/stateTaxConfigs/2026.ts`. Covers all three structural types a state tax system can take: no income tax (**TX, FL**), flat rate (**PA**, 3.07%), and progressive brackets (**CA, NY, MD**). Wired into `estimateTax`/`TaxEstimateResult` (new `stateTax` field) and the mobile dashboard, which now shows a SE/federal/state breakdown and a red warning banner if the user's state isn't in the supported list yet (never silently treats an unsupported state as $0 tax owed without flagging it). 14 new tests (8 in tax-engine, 4 in mobile glue layer, plus updated existing tests) — all passing.
  - **Confidence varies by state** — flagged explicitly in code comments: TX/FL are stable by definition; PA is high-confidence (unchanged since 2004); CA 2026 brackets are FTB-based projections (FTB hadn't published final certified 2026 figures at build time); NY 2026 figures are **provisional** (sources disagreed on the lowest five brackets after NY's FY2026 budget cut); MD is high-confidence (sourced from actual statute text).
  - 2025 config only backfills TX/FL/PA (the stable ones); CA/NY/MD under the 2025 tax year will report as unsupported until backfilled.
- [x] **MD county "piggyback" local tax** — `services/tax-engine/src/stateTaxConfigs/mdLocalTax2026.ts`. Models all 23 MD counties + Baltimore City + the flat 2.25% nonresident rate, including the two counties (Anne Arundel, Frederick) with graduated/tiered rate structures instead of a flat rate (reuses the same bracket math as state/federal). `StateTaxResult` now exposes `stateLevelTax`, `localTax`, `county`, and `localTaxSupported` separately (with `stateTax` remaining the combined total, so existing total-tax math wasn't disrupted). The dashboard shows a separate "{county} local tax" line and a distinct red warning if MD is selected but no recognized county is set — never silently drops the local tax to $0 without flagging it. Onboarding now shows a county picker (24 buttons) automatically whenever the selected state has a local tax layer, generic to any future state, not hardcoded to MD. 9 new tests added (5 tax-engine, 4 mobile), full suite green (31 tax-engine + 11 mobile), and the picker flow was verified end-to-end in a real browser session (state→county picker appears, selection persists through onboarding, dashboard shows the correct breakdown line).
  - **Confidence:** moderate — sourced from a third-party aggregator (countrytaxcalc.com), cross-checked against a Tax Foundation summary for the overall rate range and the two 2026 rate increases (Allegany, Kent), but not yet directly verified against the official Comptroller/DLS PDF tables. Flagged in code comments; verify before relying on for real filings.
  - **Known nuance:** the rate that applies is based on county of *residence*, not where the work happens — the onboarding copy says so, but there's nothing stopping a user from picking the wrong one.
- [x] **Expense tracking beyond mileage** (parking, tolls, supplies, phone business-use portion) — `Entry.expenses` in `apps/mobile/src/types.ts`, subtracted from net SE profit in `aggregateEntries` (`calculations.ts`). Mileage stays handled separately via the standard mileage rate, not as a dollar expense, since the tax engine already deducts it at the IRS rate. AddEntryScreen has a collapsed-by-default "+ Add expenses" toggle (most entries won't have any) revealing 4 fields; Dashboard shows an "Expenses logged" line and a per-entry expense note in red. 3 new tests, full suite green (13 mobile tests), verified end-to-end in a real browser session (entered $5/$3/$12/$8 across the 4 categories, confirmed the $28 total, lower tax estimate, and per-entry note all appeared correctly).
  - Note: "phone %" from the original plan language became a manual per-entry dollar amount (the business-use portion you attribute to that shift) rather than an automatic monthly-bill-times-percentage calculation — simpler for this alpha, revisit if testers want the automatic version.
- [x] Multi-platform tracking (manually tagged entries) — *already shipped in v0.2*, carrying forward as satisfied
- [x] **Quarterly estimated-tax due-date push reminders** — `apps/mobile/src/notifications/quarterlyDueDates.ts` (pure due-date math, 4 tests) + `scheduleReminders.ts` (expo-notifications wiring). Schedules a "heads up" notification 7 days before each of the next 4 quarterly due dates, plus one on the due date itself; idempotent (cancels and reschedules each call), called after onboarding completes and on every cold start that lands on the dashboard. No-ops cleanly on web (expo-notifications doesn't support reliable scheduled delivery in a browser tab) — verified via browser smoke test that this doesn't throw or block the UI.
  - **Known simplification:** due dates use the fixed Apr 15/Jun 15/Sep 15/Jan 15 rule, not adjusted for weekends/federal holidays (the IRS shifts the actual deadline to the next business day in that case). Fine for a "don't forget" nudge a few days early; don't treat the exact date as authoritative.
  - **Not verified by automated testing:** actual on-device notification delivery at a future scheduled time can't be confirmed without waiting days/months for a real due date to arrive. What's verified: the due-date math (tests) and that scheduling calls complete without error in the live API shape (confirmed against the real Expo v56 docs, since SDK 56's notification trigger API differs from older versions). Real delivery needs confirming on an actual device/Expo Go on your end.
- [x] **Basic security: biometric/PIN lock + encryption at rest** ([ROADMAP §8.2](ROADMAP.md)) — `apps/mobile/src/security/appLock.ts` + `LockScreen.tsx`, and `apps/mobile/src/storage/encryption.ts` + `cryptoCore.ts`.
  - **App lock:** Face ID/Touch ID/fingerprint via `expo-local-authentication`, with the OS's own device-passcode fallback after failed biometric attempts — this satisfies "PIN lock" without a custom in-app PIN screen, since `disableDeviceFallback` defaults to false. Locks on cold start and re-locks whenever the app returns from the background. Skips locking entirely on web or on a device with no biometrics/passcode enrolled (no native support there, and there's no security feature to enforce if the user has none set up) — verified via browser smoke test that web goes straight to onboarding/dashboard with no lock screen.
  - **Encryption at rest:** AES encryption (crypto-js) of the AsyncStorage JSON blobs, using a random 256-bit key generated once and stored in SecureStore (iOS Keychain / Android Keystore). Pure encrypt/decrypt functions split into `cryptoCore.ts` specifically so they're unit-testable without pulling in React Native's Flow-typed source (which broke Vitest's parser when crypto and key-management lived in one file) — 3 tests covering round-trip correctness, IV/salt randomness, and wrong-key failure. No-ops to plaintext on web (expo-secure-store has no web implementation) and includes a defensive fallback to read old plaintext data written before this change, so existing local test data isn't lost.
  - **Honest caveat:** this is software-level encryption with a hardware-backed key — real protection against casual disk/backup inspection, but not a substitute for an actual security audit before handling production-grade financial data at scale (same caveat the roadmap already flags for anything touching financial data).
  - **Not verified by automated testing:** real Face ID/Touch ID prompts can't be triggered headlessly. Face ID specifically does not work inside Expo Go — needs a development build to test on iOS.
**Ships to:** 20–50 closed beta testers (recruit from gig-worker subreddits/FB groups). **Exit criteria:** beta users keep using it past week 2 without prompting. **Before recruiting beta testers:** verify notification delivery and biometric unlock on a real device — both are flagged above as unverifiable by automated testing alone.

</details>

## ✅ v1.0 — Public Launch (Free Tier)

<details>
<summary>Status: Done — all engineering complete. The two tax-accuracy blockers that briefly pause…</summary>

**Status: Done** — all engineering complete. The two tax-accuracy blockers that briefly paused submission (the W2 rebuild and Head-of-Household/MFS filing-status fix) were resolved and folded back in before any App Store submission, so v1.0 shipped with them included rather than patching afterward. Manual App Store Connect actions (hosting the privacy policy, uploading screenshots, triggering the TestFlight build via the existing `codemagic.yaml`, submitting for review) are the user's to complete outside this codebase — not tracked as open engineering items.
**Goal:** first public release; free tier ships exactly as scoped, no more, no less — with the W2 feature actually correct and every filing status it claims to support modeled accurately, not just gated off or silently wrong.
- [x] **Mobile polish / premium visual pass** — `apps/mobile/src/theme.ts` (color/spacing/radius/typography/shadow tokens) + shared components (`PrimaryButton`, `Chip`, `TextField`, `Screen`) in `apps/mobile/src/components/`. All four screens (Onboarding, Dashboard, AddEntry, LockScreen) restyled on the shared token set instead of inline ad-hoc styles.
  - Safe-area handling via `react-native-safe-area-context` (`SafeAreaProvider` in `App.tsx`, `Screen` wraps every screen) — previously content could sit under the notch/status bar on real devices.
  - `KeyboardAvoidingView` added to Onboarding and AddEntry forms — previously the keyboard could cover input fields on-device.
  - `@expo/vector-icons` (Ionicons) replace plain text glyphs (`+`, `−`) throughout; per-platform icons on dashboard entry rows.
  - Dark gradient (`expo-linear-gradient`) "set aside for taxes" card for a premium fintech feel, replacing the flat tan card.
  - Light haptic feedback (`expo-haptics`) on primary button presses and chip selection — no-ops safely on web.
  - Subtle entrance fade/slide transition per screen via `Animated` — **gated to native only** (`Platform.OS !== "web"`) after discovering `useNativeDriver` animations can stall mid-transition on react-native-web (backgrounded/headless tabs throttle `requestAnimationFrame`), which left screens permanently semi-transparent. Caught via Playwright screenshot verification, not by eyeballing the dev server.
  - Verified end-to-end in a real browser session via Playwright: onboarding → dashboard → add entry → save, with a real DoorDash entry, confirming totals/tax breakdown/icons all render correctly with zero console errors. Full typecheck + 19 mobile tests still green after the change.
  - **Not yet verified:** real on-device feel (haptics, biometric-gated lock screen styling, actual notch/safe-area behavior) — browser/web verification only so far, per the project's existing browser-first dev workflow.
- [x] **📍 Expand state tax coverage from 6 states to all 50 states + DC — DONE.** All 50 states + DC are now covered in `services/tax-engine/src/stateTaxConfigs/2026.ts`. The dashboard's "not supported, $0 state tax" warning should no longer appear for any real US state.
  - [x] **No-income-tax states.** AK, NV, SD, TN, WA, WY, NH (`{ type: "none" }`, same as TX/FL) — all structurally stable, including NH (its only income tax, on interest/dividends, was fully repealed effective Jan 1, 2025) and WA (no tax on wage/SE income; its capital-gains-only excise tax is unrelated and out of scope).
  - [x] **Flat-rate states.** AZ (2.5%), IL (4.95%), MI (4.25%), CO (4.40%), GA (5.19%), IN (2.95%), KY (3.50%), NC (3.99%), UT (4.50%) — plus 5 more discovered to have converted to flat taxes recently and added in the final pass: ID (5.3%), IA (3.8%), MS (4%), OH (2.75%, new for 2026), LA (3%).
  - [x] **Progressive-bracket states + DC.** AL, AR, CT, DE, HI, KS, ME, MA, MN, MO, MT, NE, NJ, NM, ND, OK, OR, RI, SC, VT, VA, WV, WI, DC — all sourced from the Tax Foundation's "2026 State Individual Income Tax Rates and Brackets" report (published Feb 11, 2026, fetched live via WebSearch/WebFetch rather than relying on training-data knowledge, given how much accuracy matters here), the same caliber of source already used for CA/NY/MD.
  - **Real corrections found during verification, not just additions:** IN/KY/UT/NC had been shipped with stale or wrong rates in the earlier flat-rate-tier pass (IN 3.00%→2.95%, KY 4.00%→3.50%, UT 4.55%→4.50%, NC 4.25%→3.99%) — KY's and NC's corrections specifically were rate changes that took effect Jan 1, 2026 that hadn't been independently confirmed before. NC also gained its standard deduction ($12,750/$25,500) now that the real figures were sourced. This is exactly why the earlier "lower confidence, verify before relying on" flags existed — verifying caught real, materially-wrong numbers.
  - **Disclosed simplification (structural, not a sourcing gap):** states whose only "personal exemption" is a tax *credit* (not a deduction) aren't modeled — this engine has no state-level tax-credit mechanism, only deduction-style reductions to taxable income (affects AR, DE, NE, OR, GA's/MN's/SC's *dependent* credit specifically, UT's "taxpayer tax credit", plus NM's per-dependent deduction). This overstates those states'/taxpayers' tax very slightly, the safer direction of error. Where a state's exemption *is* deduction-style, it's folded into `standardDeduction` (AL, CT, HI, KS, ME, MA, NJ, OK, RI, VT, VA, WI, WV).
  - [x] **Deferred flat-rate standard-deduction gap — CLOSED.** The original 9-state flat-rate tier shipped with no `standardDeduction` modeled at all; now unblocked and closed (per your call to finish it once the rest of the 50-state work landed). AZ ($16,100/$32,200, conforms to federal), IL ($2,925/$5,850, per-person exemption), MI ($5,900/$11,800, per-person exemption), CO ($16,100/$32,200, approximates the federal-taxable-income basis Colorado actually uses), GA ($12,000/$24,000), IN ($1,000/$2,000, per-person exemption), KY ($3,360/$3,360 — correct per actual KY law, not doubled for joint filers) all now have real figures. UT genuinely stays without one — its exemption is a tax *credit*, not a deduction, a structural gap rather than a sourcing one. Verified in a real browser session: Georgia's deduction now correctly lowers its estimate ($1,788.87 state tax on $50k, down from what it would have shown without the deduction), matching hand-checked arithmetic exactly. 54 tax-engine tests passing (1 new).
  - **Known follow-on complexity, separate from this item:** several states have *local/municipal* income tax beyond the state level — MD's county piggyback tax and PA's/NY's local tax (Philadelphia/Pittsburgh/NYC) are now modeled (see the "PA and NYC local tax silently missing — CLOSED" item below); Ohio's near-universal municipal income tax (RITA/CCA) is not yet modeled for any OH jurisdiction.
  - Updated three existing tests that had used now-supported states (OH, WA) as "this is unsupported" examples — swapped to PR (Puerto Rico), a real US territory genuinely not modeled, since literally every state + DC is covered now.
- [x] **📍 State tax confidence gaps found during/after the 50-state rollout — ALL CLOSED.** A full audit pass after completing the 50-state work surfaced several distinct things that affected how much to trust the numbers, beyond what was already disclosed inline in `stateTaxConfigs/2026.ts`'s doc comment. All 5 sub-items below are now closed; remaining lower-priority items (CA/NY provisional brackets, MD's third-party-sourced county data, an annual re-verification cadence) are tracked as their own still-open notes within this section rather than as blocking gaps.
  - [x] **State-level tax credits — CLOSED.** New `StateCreditConfig` on `StateTaxConfig`: a generic nonrefundable flat per-filer and/or per-dependent credit, applied against `stateLevelTax` only (never local tax), capped at the tax otherwise owed — mirrors exactly how the federal Child Tax Credit only offsets income tax, not SE tax, and reuses the same `numberOfChildren` count already plumbed through for that. New `creditApplied` field on `StateTaxResult` (gross `stateLevelTax` stays pre-credit, same convention as `federalIncomeTax.incomeTax`, so the UI can show a separate line). Modeled for UT (approximated flat $966/$1,932, since Utah's real formula — 6% of a federal-exemption-equivalent with its own phase-out — isn't replicated), AR/DE/NE/OR (small flat credits, $29–$256), and the genuinely material ones: **GA ($4,000/dependent), MN ($5,300/dependent), SC ($4,930/dependent)**. NM's per-dependent amount stays unmodeled — it's a deduction, not a credit, with an irregular "$4,000 for all but one dependent" rule that didn't fit either mechanism cleanly. The mobile app needed zero changes to wire this up — `apps/mobile/src/calculations.ts` already passed `taxProfile.dependents` through as `numberOfChildren` for the federal CTC. New dashboard breakdown line: "{state} state tax credit," shown whenever `creditApplied > 0`.
    - 59 tax-engine tests passing (5 new, covering GA's material credit, MN/SC's per-dependent credits, AR's combined per-filer+per-dependent credit, the nonrefundable floor-at-zero behavior, and the default-to-zero-dependents case). 36 mobile tests unaffected.
    - Verified end-to-end in a real browser session: Georgia + 2 dependents + $200k income showed "GA state tax credit −$8,000.00" alongside the existing federal CTC line, with the headline total matching hand-checked arithmetic exactly.
  - [x] **2025 was missing ~40 states that 2026 has — CLOSED.** Backfilled all 50 states + DC in `taxYears/2025.ts`, sourced from the Tax Foundation's "2025 State Individual Income Tax Rates and Brackets" report, cross-checked against several states' own DOR sites where figures looked uncertain. Confirmed several real structural differences from 2026 (not just inflation drift): GA/KY/IN/UT/ID/MS rate phase-downs already in progress between 2025 and 2026, MT/NE multi-year bracket consolidations, OK's 2026 bracket simplification, and Ohio's 2026 transition from two-rate brackets to a single flat rate (modeled as `type: "bracket"` for 2025 specifically, `type: "flat"` for 2026). Maryland's 10-bracket structure confirmed identical between years since it was made retroactively effective for tax year 2025 by statute. PA/NY/MD local tax wired up for 2025 too (reusing the 2026 jurisdiction maps), since leaving it unset would have silently reintroduced the exact "silent $0, no warning" bug just fixed for 2026.
    - **Found and fixed a more serious, unrelated bug while researching this:** the federal standard deduction in `taxYears/2025.ts` was the stale pre-OBBBA Rev. Proc. 2024-40 figure ($15,000/$30,000). The One Big Beautiful Bill Act (signed July 2025) retroactively raised the 2025 standard deduction to $15,750/$31,500 — this was wrong for every single 2025 entry regardless of state, not just a missing-state gap. Fixed, with 3 existing tests updated whose hand-verified assertions were built against the stale figure.
    - **Newly discovered, flagged for a future correction pass rather than fixed now (scope):** 2026.ts's ME `standardDeduction` doesn't look like it matches Maine's actual federal-conforming structure (Maine conforms to the federal standard deduction plus its own ~$5,150/dependent exemption, not a separate $8,350/$16,700 state figure); 2026.ts's LA `standardDeduction` ($14,600/$29,200) looks like it may have been set to the federal conformity figure rather than Louisiana's own deduction, which 2025's verified figure ($12,500/$25,000) suggests is meaningfully different.
    - 7 new tests (73 tax-engine total): full 2025 50-state+DC coverage, GA's pre-phase-down rate, KY's non-doubled flat deduction, OH's 2025 bracket structure, MD's retroactive-law year-to-year consistency, Philadelphia local tax wired for 2025, and the OBBBA-corrected standard deduction. 36 mobile tests unaffected.
    - Verified in a real browser session that a 2025-dated entry is accepted and correctly excluded from the current-year (2026) dashboard aggregate — full dashboard-level verification of the 2025 estimate itself isn't possible without a tax-year switcher (the dashboard always shows the current calendar year, a pre-existing UI gap, not something this item needed to fix); the actual 2025 tax math is hand-verified via the unit test suite using the identical `calculateStateTax`/`estimateTax` code path the dashboard calls.
  - [x] **PA and NYC local tax silently missing — CLOSED.** New `services/tax-engine/src/stateTaxConfigs/paLocalTax2026.ts` and `nyLocalTax2026.ts`, wired into PA's and NY's `localTaxJurisdictions` in `2026.ts`. Covers the two highest-impact jurisdictions per state rather than every municipality: Philadelphia (3.72% blended flat Wage Tax — approximated since the real rate changes mid-year) and Pittsburgh (3.0% flat combined city+school EIT) for PA; New York City's verified 4-bracket resident tax (3.078%–3.876%, single thresholds $12k/$25k/$50k, MFJ $21.6k/$45k/$90k — confirmed not simply double the single thresholds) for NY. Any other PA municipality or NY locality still correctly falls through to the existing `localTaxSupported: false` warning instead of silence — same honest-uncertainty pattern as MD's unrecognized-county case, just applied to PA/NY now. 7 new tests (66 tax-engine total, 36 mobile unaffected). Verified end-to-end in a real browser session: PA→Philadelphia and NY→New York City both show a correct local tax breakdown line with hand-checked arithmetic, and onboarding still requires a county/city selection before continuing (same enforcement as MD).
  - [x] **Most of the 24 new progressive-bracket states had no dedicated test beyond the initial data entry — CLOSED.** Added hand-worked bracket-math tests for all 19 previously-untested states (CT, HI, KS, ME, MN, MO, MT, NE, NJ, NM, ND, OK, OR, RI, SC, VT, VA, WV, WI) — each picks an income crossing at least 2 real bracket boundaries and asserts the worked-by-hand expected tax, the same rigor AL/MA/DE/DC already had.
  - [x] **Reverification pass for already-scheduled rate changes (NC, MT, KY, IN, OK, CO) plus ME and LA — CLOSED.** Checked all 8 states against current authoritative sources (state DOR sites, Tax Foundation's 2026 report, recent legislative reporting):
    - **NC, MT, KY, IN, OK, CO: all confirmed accurate as already configured, no code changes needed.** NC's 2027 cut to 3.49% is now confirmed proceeding (a May 2026 budget deal resolved the earlier freeze threat, just stretched the further-cuts timeline out to 2034). MT's 2026 figures (4.7%/5.65% @ $47,500/$95,000) match HB 337 exactly, with the 2027 step to 5.4% confirmed real law. KY's 3.5% and IN's 2.95% confirmed correct for 2026, both with confirmed 2027 next-step schedules. OK's 4-bracket 0%/2.5%/3.5%/4.5% structure confirmed via Tax Foundation's table, matching exactly despite conflicting secondary sources. CO's 4.40% confirmed correct for both 2025 and 2026 (the temporary TABOR-triggered dip to 4.25% was tax year 2024 specifically, not 2025 — resolved a conflicting secondary source against EY/Deloitte/the actual legislative bill text).
    - **ME and LA: both had real, pre-existing bugs, now fixed.** ME's `standardDeduction` was built on a wrong structural assumption (that Maine conforms to the federal standard deduction) — Maine actually has its own COLA-indexed figure, confirmed directly against Maine Revenue Services as $15,300/$30,600 for 2026 (was wrongly $8,350/$16,700). Also added Maine's new 2% surcharge bracket on taxable income over $1M/$1.5M (signed for tax years beginning 2026), modeled the same way as MA's existing millionaire's surtax. LA's `standardDeduction` had been set to the federal conformity figure by mistake ($14,600/$29,200) instead of Louisiana's own deduction ($12,875/$25,750 for 2026, confirmed via Tax Foundation). The same wrong ME assumption was also fixed in the 2025 backfill.
    - 20 new tests (19 for the previously-untested states + 1 for ME's surcharge bracket), 2 existing tests corrected (LA's and ME's deduction figures) — 74 tax-engine tests total, 36 mobile unaffected. Verified ME ($60k single, $15,102.15 total) and LA ($50k single, $11,468.67 total) end-to-end in a real browser session with hand-checked arithmetic matching exactly.
  - [x] **ME's standard deduction phaseout — CLOSED.** New `StandardDeductionPhaseout` on `BracketStateTax` (`services/tax-engine/src/types.ts`): reduces `standardDeduction` by `deduction * min(1, max(0, (MAGI - threshold) / additionalLimit))`, mirroring Maine's own published worksheet formula (36 M.R.S. 5124-C(2)/5125(7)) exactly — confirmed directly against MRS's "2026 Estimated Tax Worksheet, Line 6a" PDF (threshold $102,250 single/$204,550 MFJ, additional limit $75,000/$150,000) and the equivalent 2025 worksheet ($100,000/$200,050 threshold, same $75,000/$150,000 limit). Wired into `calculateStateTax` in `stateTax.ts` for both `2025.ts` and `2026.ts`'s ME configs. **Known simplification:** Maine's personal exemption (folded into the same combined `standardDeduction` figure since the engine has no separate exemption field) actually phases out under a different statute (36 M.R.S. 5126-A) with much higher thresholds (~$125k/$250k-area divisors) — applying the lower deduction threshold to the combined figure overstates tax slightly at high incomes rather than understating it, consistent with this codebase's existing safer-direction bias. 1 existing test corrected (a $1.02M earner's deduction is now fully phased out to $0) + 1 new test added for the partial-phaseout range — 74 tax-engine tests passing, 36 mobile unaffected. Verified end-to-end in a real browser session: a $142,250 ME entry showed $8,047.92 state tax, matching hand-checked arithmetic (MAGI ≈$132,200 after the SE-tax deduction → ~40% into the phaseout window → deduction reduced from $20,600 to ≈$12,374) to the penny.
  - [x] **CA's 2026 (and 2025) brackets/standard deduction — CLOSED.** Verified directly against the EDD's official "California Withholding Schedules for 2026" PDF (edd.ca.gov), cross-checked against FTB-sourced 2025 figures reported by NerdWallet and the Tax Foundation. Found and fixed **two real, distinct bugs**, not just confirmed the existing placeholder: (1) both `2025.ts`'s and `2026.ts`'s CA bracket thresholds and standard deduction were stale by about a year — they matched 2024's confirmed figures, not 2025/2026's (e.g. the 9.3% bracket started at $70,612 in code vs. the confirmed $72,724; standard deduction was $5,540/$11,080, a full year behind the confirmed $5,706/$11,412). (2) `2026.ts`'s married-filing-jointly bracket had a structural error: it doubled the $1M Mental Health Services Tax surcharge threshold to $2M for joint filers, contradicting the engine's own documented design (the surcharge is explicitly flat/non-doubled, same as for single filers) — fixed by adding an extra bracket "kink" that splits exactly at $1,000,000, since that threshold falls in the middle of CA's 11.3% statutory bracket for MFJ ($891,542-$1,485,906). `2025.ts` already had the correct flat-threshold structure, just with stale-by-a-year numbers. 1 existing test corrected, 1 new test added specifically for the MFJ $1M-kink fix — 75 tax-engine tests passing. Verified end-to-end in a real browser session: a $1,211,412 CA MFJ entry showed $110,730.01 CA state tax, matching hand-checked bracket arithmetic (≈$110,722, within rounding) and confirming income above $1M is now correctly taxed at 12.3% (11.3% + 1% surcharge) rather than the old buggy 11.3%.
  - [x] **NY's 2026 "provisional" brackets — CLOSED, no bug found.** Verified directly against the NYS Department of Taxation and Finance's official "NYS-50-T-NYS (1/26)" withholding publication (bracket dollar thresholds match the code exactly) and a Grant Thornton legal summary of the actual enacting law (Chapter 59 of the Laws of 2025, Part A / A3009). The real cut is exactly -0.1 percentage point on the bottom five sub-brackets below $215,400 single/$323,200 MFJ (4%→3.9%, 4.5%→4.4%, 5.25%→5.15%, 5.5%→5.4%, 6%→5.9%, with a further -0.1 step legislated for 2027), and the existing code already had exactly these rates and thresholds. The original "provisional" flag stemmed from an unresolved disagreement in secondary sources about whether the cut was -0.1 or -0.2 per bracket — now resolved in favor of the figures already in code. No code changes needed, just updated the doc comment from "provisional, verify" to "confirmed."
  - [x] **MD's third-party-sourced county data — CLOSED, no bug found.** Verified directly against the Department of Legislative Services' official "Local Tax Rates" publication (sourced from the Office of the Comptroller) and the Comptroller's "Withholding Tax Facts, January 2026" (COM RAD 098) for Anne Arundel's and Frederick's graduated brackets. All 24 jurisdictions (21 flat-rate counties + Baltimore City, Anne Arundel's and Frederick's 3-tier graduated brackets, and the nonresident rate) matched the official source exactly — the third-party aggregator data held up. No code changes needed, just updated the doc comment in `mdLocalTax2026.ts` from "not yet cross-checked" to "confirmed."

**All 5 originally-deferred lower-priority confidence-gap items are now closed** (ME's deduction phaseout, CA's brackets — where 2 real bugs were found and fixed, NY's brackets, and MD's county data — both confirmed correct with no changes needed). The only remaining open item from this whole audit is the not-yet-set-up annual reverification cadence (per ROADMAP §6).
  - [x] **Annual re-verification cadence — SET UP.** [TAX_CONFIG_REVIEW.md](TAX_CONFIG_REVIEW.md) is a runbook (not automated code, per the nature of the task — verifying a tax figure against a government source needs judgment) covering: when to run it (Dec/Jan, since federal/state publication timing varies), a step-by-step process for federal figures, a step-by-step process for state figures (including a checklist of specific known scheduled changes to check: KY/IN's phase-downs, NC's 2027 cut, MT's 2027 step, NY's next -0.1 step, ME's annual COLA re-indexing, MD's county rates), the WebFetch-PDF-then-local-Read technique for stubborn government PDFs discovered this session, a caveat about withholding tables using blended (non-statutory) rates, the existing implement/test/verify/commit pattern, and a review log table to track when each pass happened and what changed.
  - 53 tax-engine tests passing (4 new: threshold-as-deduction flat states, NC's deduction, a representative bracket-state sample including MA's millionaire's-surtax threshold, and DC). 36 mobile tests passing.
  - Verified in real browser sessions: Wyoming $0.00 with no warning; Illinois $2,300.15 on $50k matching hand-checked arithmetic; Massachusetts $3,032.73 on $70k matching hand-checked arithmetic (confirming the surtax threshold logic works below the $1,083,150 cutoff).
- [x] **🔴 Critical: entries are never scoped to a tax year — FIXED.** `services/tax-engine/src/index.ts` now exports `taxYearConfigs: Record<number, TaxYearConfig>` alongside the existing `currentTaxYear`. `apps/mobile/src/calculations.ts`'s `computeTaxEstimate(entries, taxProfile, year = currentCalendarYear)` filters entries to that calendar year via a new `entriesForYear()` helper before aggregating, and looks up the matching config from `taxYearConfigs[year]` instead of always using the global `currentTaxYear` — falling back to `currentTaxYear` (with a `usedFallbackConfig` flag) only if no config exists yet for the requested year. `DashboardScreen` now scopes its headline "Total earnings logged" and "Set aside for taxes" numbers to that same year, shows a `{year}` badge next to "Your earnings" so the scoping is visible, and shows a red warning banner if `usedFallbackConfig` is true (next year's rates aren't published yet). The "Recent entries" list intentionally still shows full history (it's a log, not a calculation) — only the headline numbers are year-scoped.
  - 6 new tests added to `calculations.test.ts` (year-exclusion, fallback-config flagging, `entriesForYear`/`yearsWithEntries` helpers) — full suite green (25 mobile + 31 tax-engine tests).
  - Verified end-to-end in a real browser session: logged a 2026 DoorDash entry ($120) and a 2020 Uber entry ($99,999) — the dashboard's totals/tax estimate stayed at $120/$16.96 (2026-only), while "Recent entries" correctly showed both, confirming the prior-year entry doesn't leak into the current year's math.
  - **Known follow-up, not done here:** there's still no UI to *view* a prior year's totals (e.g. switch the dashboard to 2025) — it only ever shows the current calendar year. That's the "selectable tax year" half of the original gap description; tracked as a smaller fast-follow, not blocking, since the correctness bug (mixing years together) is what's fixed.
- [x] **Quarterly due dates + a catch-up calculator — DONE.** Both gaps closed together as one Dashboard feature. New `computeCatchUpStatus()` (`apps/mobile/src/calculations.ts`) is a pure, testable function (mirrors the `w2WithholdingYearFraction` convention of taking `now` as a parameter instead of reading the clock internally) that compares a self-reported "amount set aside so far" against the existing `netAmountToSetAside` figure and, if there's a shortfall, spreads the gap across the weeks remaining until the next date from `getUpcomingQuarterlyDueDates()` (`apps/mobile/src/notifications/quarterlyDueDates.ts`) — finally giving that function a UI consumer beyond the push-notification scheduler. New `TaxProfile.amountSetAsideByYear?: Record<number, number>` (`apps/mobile/src/types.ts`) stores the self-reported figure per tax year (it resets conceptually every year, so a flat field would have been wrong) — persists automatically through the existing `saveTaxProfile`/`getTaxProfile`, no new storage key needed. New `handleUpdateAmountSetAside()` in `App.tsx` saves it without navigating away (unlike `handleSaveTaxProfile`, which returns to Settings). `DashboardScreen` gained a new card below the existing "Set aside for taxes" gradient card: an inline editable `$` amount (save-on-blur or via a checkmark button, not on every keystroke), the next due date/label, and a status message that's either green/reassuring ("on track" or "$X more than you need, nice work") or red/actionable ("$X behind — set aside an extra $Y/week until [date] to catch up").
  - 5 new tax-engine-adjacent unit tests in `calculations.test.ts` (exactly caught up, ahead, behind with a multi-week runway, the due-date-already-passed edge case where weeks-remaining floors at 1 instead of going negative, and the no-due-date defensive case) — 41 mobile tests passing (36 + 5). No tax-engine changes, so its 75 tests are unaffected. Full typecheck clean.
  - Verified end-to-end in a real browser session (TX, a $50,000 entry, $10,460.89 owed): with $0 set aside, showed "$10,460.89 behind — set aside an extra $871.74/week until Sep 15, 2026" (hand-checked: 82 days ÷ 7 = 12 weeks remaining, $10,460.89 ÷ 12 = $871.74, exact match); entering $1,000 updated to "$9,460.89 behind ... $788.41/week" (exact match); entering $50,000 switched to the green "You've saved $39,539.11 more than you need so far" state (exact match: $50,000 − $10,460.89).
  - **Known simplification, consistent with the rest of this feature area:** still a single self-reported running total, not a full ledger of individual payments made — exactly the scope the original gap description called for ("a single running 'amount set aside' number is enough for v1, not a full ledger").
- [x] **`dependents` is collected during onboarding but never used in any tax calculation — FIXED, by implementing real Child Tax Credit support.** New `services/tax-engine/src/childTaxCredit.ts`: nonrefundable credit ($2,200/child for 2025/2026 per the OBBBA-set permanent figure) applied against federal income tax first, plus a refundable Additional Child Tax Credit (capped at $1,700/child and at 15% of earned income over $2,500) that can offset total tax owed — including SE tax — even when income tax liability is already zero, which is the scenario that matters most for lower-income gig workers. Basic high-income phase-out included ($200k single/$400k MFJ, −$50 per $1,000 over). `TaxYearConfig` gained a `childTaxCredit` config block (added to both `taxYear2025` and `taxYear2026`); `TaxEstimateInput` gained optional `numberOfChildren` (defaults to 0, so omitting it is a no-op — existing callers unaffected); `TaxEstimateResult` gained a `childTaxCredit` field. `apps/mobile/src/calculations.ts` now passes `taxProfile.dependents` through as `numberOfChildren`, and the dashboard shows a green "Child Tax Credit (N) −$X" breakdown line whenever it's nonzero.
  - **Known simplification, flagged in code:** every declared dependent is treated as a CTC-qualifying child under 17 — the real IRS rules give non-qualifying dependents (e.g. an elderly parent) only the smaller, separate $500 Credit for Other Dependents, which isn't modeled.
  - **Confidence:** phase-out mechanics and the refundable cap are carried forward from pre-OBBBA/2025 figures into the 2026 config since the IRS hadn't published the 2026 inflation-indexed amount at build time — flagged in code comments the same way the provisional NY brackets already are; verify before relying on for real filings.
  - 9 new tax-engine tests (6 unit tests on the pure `calculateChildTaxCredit` function covering zero-children, full nonrefundable absorption, refundable-capped-by-per-child-cap, refundable-capped-by-earned-income, full phase-out, and partial phase-out; 3 integration tests on `estimateTax` confirming SE tax is never reduced by the credit, the credit nets correctly into `totalEstimatedTax`, and the refundable case floors at zero) — full suite green (40 tax-engine tests). 1 new mobile regression test confirming `dependents` now actually changes the estimate (the original bug) — full suite green (26 mobile tests).
  - Verified end-to-end in a real browser session: onboarded with 2 dependents and an $80,000 entry, dashboard showed "Child Tax Credit (2) −$4,400.00" and the total matched hand-checked arithmetic (SE tax + federal income tax − credit + state tax) exactly.
  - **Not in scope here:** state-level dependent exemptions/credits (most states use a different mechanism than the federal CTC) — left for the existing 50-state-coverage work, not duplicated as a separate item.
- [x] **W2 income was correctly pushing SE income into the right tax bracket, but never credited back tax already withheld from paychecks — FIXED.** This was the exact gap [ROADMAP §8.1](ROADMAP.md) already named ("W2 withholding already covers part of their tax bill, so the set aside math is wrong without this") but it had never actually been addressed — W2+gig users were being told to set aside money for tax their employer was already withholding. New `services/tax-engine/src/w2Withholding.ts`: `estimateW2Withholding()` computes full-year federal+state tax on the W2 income in isolation (as if it were the only income) — a proxy for what a correctly-filled-out W-4 targets withholding at, since IRS withholding tables assume single-job income. Wired into `estimateTax()` as a new `w2WithholdingEstimate` field on `TaxEstimateResult`; deliberately doesn't touch the existing (correct) use of the full annual W2 figure for bracket-pushing.
  - `apps/mobile/src/calculations.ts` does the date-dependent proration the engine can't (pure functions shouldn't read the clock): `w2WithholdingYearFraction(year, w2EndDate?, now?)` computes what fraction of the job's active period (Jan 1 through `w2EndDate`, or year-end if none) has elapsed, multiplies it by the annual estimate to get a year-to-date credit, and `computeTaxEstimate()` now returns both `w2WithholdingYtdEstimate` and `netAmountToSetAside` (the gross total minus that credit, floored at 0) alongside the existing gross figure.
  - **Also added, since it came up in the same conversation:** an optional `w2EndDate` on `TaxProfile` so calculations account for a W2 job that doesn't run the full year — a job that ended in a *prior* tax year zeroes out its income/withholding contribution entirely for the current year, not just the withholding credit.
  - **Also added:** replaced the "Estimated annual W2 income" field in `OnboardingScreen`/`EditTaxProfileScreen` with **paycheck amount + pay frequency** (weekly/biweekly/semi-monthly/monthly) plus a computed "≈ $X/year" hint — most people know their regular paycheck amount, not their annual gross. `estimatedW2Income` remains the canonical annual figure every downstream consumer reads; `w2PaycheckAmount`/`w2PayFrequency` are stored purely so the edit form can show the user's paycheck back to them next time instead of making them reverse-calculate it. The edit screen falls back to a best-guess reverse conversion (`annual ÷ 26`) for profiles saved before this existed.
  - Dashboard's headline "Set aside for taxes" now shows `netAmountToSetAside` instead of the gross total (with the displayed set-aside percentage recalculated to match), plus a new green "W2 withholding so far (est.)" breakdown line.
  - 7 new tax-engine tests (`w2Withholding.test.ts`) + 2 new integration tests in `estimate.test.ts` (confirming the estimate is unaffected by how much SE income is layered on top, and is 0 with no other income) — 47 tax-engine tests passing. 10 new mobile tests (paycheck-frequency conversion, year-fraction proration including the prior-year-end-date edge case, and the full `computeTaxEstimate` integration) — 36 mobile tests passing. Full typecheck clean both places.
  - Verified end-to-end in a real browser session: $2,000 biweekly correctly showed "≈ $52,000/year"; with a $30k DoorDash entry, the dashboard showed the gross SE+federal+state total minus a $1,945.11 withholding credit, matching hand-checked arithmetic exactly. Confirmed the edit screen round-trips the saved paycheck amount/frequency correctly.
  - **Simplification, flagged in code:** assumes withholding is evenly spread across the job's active period from an accurate W-4 — real per-paycheck withholding can be lumpy or based on a stale/incorrect W-4. This is a planning estimate, not a substitute for checking actual paystub withholding.
  - **FOLLOW-UP (real-device testing): three compounding bugs found, feature DISABLED, full rebuild scoped and now folded back into this same v1.0 — see the rebuild item below.** A pure-W2 user (no gig income) was shown a misleading "set aside $1,496.93" when it should have been close to $0. Root causes: (1) this entry's own withholding-credit logic only prorates by *elapsed time*, crediting back less than the full year's expected withholding even though future paychecks keep withholding automatically — the "gap" shown isn't money the user is actually on the hook for; (2) the "Paycheck amount" field is explicitly labeled "take-home pay" in the UI but is fed into the tax engine as gross taxable income, when take-home already has tax/FICA/401k/insurance removed; (3) `seTax.ts` doesn't account for W2 wages at all when computing the SE-tax Social Security wage-base cap or Additional Medicare Tax threshold, both of which are wrong (in opposite directions) for anyone with combined W2+SE income. Rather than patch this right before launch, the W2 UI is disabled via a new `apps/mobile/src/featureFlags.ts` (`W2_JOB_SUPPORT_ENABLED = false`) so no real user can hit the bug — `OnboardingScreen.tsx`'s and `EditTaxProfileScreen.tsx`'s W2 toggle/fields are gated behind it. Deliberately did NOT delete the underlying logic in `calculations.ts`/`w2Withholding.ts` or its tests (all still pass, since they're simply unreachable now that the UI can't set `hasW2Job: true`) — that work is the starting point for the rebuild below. Verified in a real browser session: the W2 toggle no longer appears in onboarding or Settings → Tax Profile.
- [x] **Default entry date uses UTC, not local time — FIXED.** `todayIsoDate()` in `AddEntryScreen.tsx` now builds the default from local `getFullYear()`/`getMonth()`/`getDate()` instead of `toISOString().slice(0, 10)`. Verified analytically (a fixed UTC instant that's still "yesterday" in `America/New_York` now correctly resolves to yesterday's local date instead of today's UTC date) and in a live browser session (default field matched the browser's own locally-reported date). While investigating, also fixed a related **flaky test** in `encryption.test.ts`: `decryptText` with a wrong key can legitimately throw (CryptoJS's UTF-8 decode fails on most, but not all, random garbage byte sequences) rather than reliably returning a string — the test now accepts either outcome, since the actual invariant is "never recovers the original plaintext," not "always returns a string." No code in `cryptoCore.ts` itself changed.
- [x] **Edit/delete logged entries — FIXED.** New `updateEntry()` in `apps/mobile/src/storage/repository.ts` alongside the existing `deleteEntry()`. `AddEntryScreen` now does double duty: an optional `entry` prop switches it into edit mode (prefills every field, preserves the original `id`/`createdAt` on save, title becomes "Edit Entry," save button becomes "Save Changes," and a trash icon appears in the header). Tapping any row in the dashboard's "Recent entries" list (now a `Pressable` with a chevron affordance) opens it in edit mode; `App.tsx` tracks `editingEntry` and routes `handleSaveEntry` to `updateEntry` vs `addEntry` accordingly. Delete asks for confirmation via `Alert.alert` (destructive-style button) before calling the new `handleDeleteEntry`. Added `accessibilityLabel`/`accessibilityRole` to the close and delete icon buttons while touching this screen (closes part of the separately-tracked accessibility-labels gap below).
  - Verified end-to-end in a real browser session: logged a $100 DoorDash entry, tapped it, confirmed the edit screen was correctly prefilled, changed gross pay to $250, saved, and confirmed the dashboard total updated to exactly $250 (not a duplicate entry).
  - **Delete confirmation not verified by browser testing — found a real platform limitation, not a bug in this feature:** `Alert.alert` is a complete no-op on React Native Web in this project's dev setup (no dialog, no console output, nothing) — confirmed by testing that even the already-shipped, pre-existing "Check gross pay" validation alert produces zero visible effect on web. This affects every `Alert.alert` call in the app, not just the new delete confirmation; it's a known web-dev-mode gap, same category as the already-flagged biometric/notification on-device-only verification items. The delete *logic* (button → confirmation → `onDelete(entry.id)` → `deleteEntry()` → state update) follows the identical, already-trusted `Alert.alert` pattern used elsewhere in this codebase, but needs confirming on a real device/Expo Go before launch.
  - Full suite green (typecheck clean, 26 mobile tests — no new automated tests added here since `repository.ts` has no existing test coverage precedent, same reason as `cryptoCore.ts` being split out for testability; this is UI+storage wiring verified via the browser session above instead).
- [x] **Settings screen — DONE.** `apps/mobile/src/screens/SettingsScreen.tsx` (reachable via a gear icon on the dashboard) now covers all three originally-missing pieces: inline profile editing (name/email, same validation as onboarding), a "Tax Profile" summary row that opens the new `EditTaxProfileScreen.tsx` (filing status/dependents/state/county/W2 job+income, mirroring onboarding's fields), and a "Clear All Data" destructive action that finally wires up `clearAllLocalData()` — satisfying the CCPA-style account/data-deletion requirement from [ROADMAP §8.2](ROADMAP.md). The App Lock toggle from the earlier item lives here too.
  - **Bug found and fixed while building the edit version:** onboarding's county-reset `useEffect` cleared the selected county on every mount regardless of whether it was still valid for the current state — harmless in onboarding (nothing to reset yet), but would have silently wiped a perfectly valid pre-filled county the instant `EditTaxProfileScreen` opened. Fixed to only reset when the county no longer matches the (possibly newly typed) state; onboarding's version is unchanged since it never had a pre-filled value for this to affect.
  - Verified end-to-end in a real browser session: edited profile name and tax profile (switched to MFJ, MD, Montgomery County), confirmed both persisted and the dashboard's breakdown immediately showed the new state/county tax lines.
  - **Not verified by browser testing:** the actual "Clear All Data" destructive flow — the button and confirmation dialog render correctly, but `Alert.alert` is a known no-op on React Native Web in this project (see the v1.0 polish-pass notes above), so the confirmation can't be clicked through on web. Needs a real-device check before relying on it.
- [x] **Native date picker for entry date — DONE.** New `DateField` component, split per-platform since `@react-native-community/datetimepicker` (confirmed against the Expo v56 SDK docs, per this app's own AGENTS.md instruction to check exact versioned docs) has **no web support at all** — `DateField.tsx` (iOS/Android, opens the native system date picker via a `Pressable`) and `DateField.web.tsx` (a plain browser `<input type="date">`, since this codebase's primary dev/verification loop is the web build), with Metro automatically resolving the right one per platform. Both cap the max selectable date at today (`maximumDate`/`max`), closing the "no future-date validation" half of the original gap directly in the picker UI rather than as a separate after-the-fact check; the leap-day/month-length half is closed for free since neither a native picker nor a browser date input can produce an invalid calendar date to begin with. Extracted the existing local-date (not UTC) helpers out of `AddEntryScreen.tsx` into a new shared `apps/mobile/src/dateUtils.ts` (`todayIsoDate`, `toIsoDateLocal`, `parseIsoDateLocal`) since both new files need the same logic.
  - `npx expo install @react-native-community/datetimepicker` added the package + wired its config plugin into `app.json` automatically.
  - Full suite green (41 mobile tests unaffected — this was a UI-only swap, no new pure logic to unit test; tax-engine's 75 tests untouched) and both typechecks clean. The library's actual `onChange`/`DateTimePickerEvent` API shape was confirmed by the TypeScript compiler itself rather than trusted from a web search summary, since dense library docs have been unreliable to fetch verbatim earlier this session.
  - Verified end-to-end in a real browser session: the web date input defaults to today's local date, its `max` attribute is correctly capped at today (confirmed via direct attribute inspection, not just visually), setting an explicit past date (`2026-03-10`) saves correctly and shows in the entries list, and reopening that entry for editing round-trips the same date back into the picker.
  - **Not verified by browser testing, by necessity:** the native iOS/Android picker UI itself (`DateField.tsx`) can only be exercised on a real device/simulator — same category as this project's other already-disclosed native-only verification gaps. The web path, which is what every automated check in this repo actually exercises, is fully verified.
- [x] **App-level error boundary — DONE.** New `apps/mobile/src/components/ErrorBoundary.tsx` (a class component, as required for React error boundaries) wraps `AppContent` in `App.tsx`. Catches render/lifecycle errors app-wide and shows a recoverable fallback (error message, a "your data is safe, it's all local" reassurance, and a "Try Again" button that resets the boundary) instead of a blank white screen. Only catches render-phase errors — async failures in event handlers are a different category, already handled via the try/catch + `Alert.alert` pattern added earlier at each call site in `App.tsx`. Still pairs with the separately-planned "Crash reporting" item below for actually logging these occurrences remotely, which this doesn't do.
  - Verified by temporarily forcing `DashboardScreen` to throw and confirming the fallback rendered with the right message and a working "Try Again" button, then reverted the test throw and re-confirmed a clean typecheck/test run. Full suite green (36 mobile tests).
- [x] **Accessibility labels on icon-only controls — DONE.** Auditing every `Pressable` app-wide found that the specific controls this item originally named (close/delete icon buttons in `AddEntryScreen`, `SettingsScreen`, `EditTaxProfileScreen`) had already been fixed during the earlier "Edit/delete logged entries" and "Settings screen" work — but two much higher-traffic gaps were still open: **`Chip`** (`apps/mobile/src/components/Chip.tsx`, used for every filing-status/platform/county/frequency selection across onboarding, entry logging, and settings) and **`PrimaryButton`** (`apps/mobile/src/components/PrimaryButton.tsx`, used for nearly every primary action in the app) had no `accessibilityRole` at all — a broader, more impactful version of the same underlying problem than the literal "icon-only" framing suggested. Fixed both: `Chip` gained `accessibilityRole="button"` + `accessibilityState={{ selected }}`; `PrimaryButton` gained `accessibilityRole="button"`, `accessibilityLabel={label}`, and `accessibilityState={{ disabled, busy: loading }}`. Also added `accessibilityRole="button"` + `accessibilityState={{ expanded }}` to `AddEntryScreen`'s "Add expenses" toggle, the one genuinely-missed control from the original audit.
  - **Verified in a real browser session** that the core fix actually works: Playwright's `getByRole("button", { name: ... })` now successfully finds the "Continue" `PrimaryButton` and the "Single" `Chip` by their accessible role+name — previously neither had a discoverable role at all.
  - **Honest limitation found during verification, not swept under the rug:** the supplementary `accessibilityState` values (`selected`, `expanded`) are correctly passed to React Native's accessibility API (confirmed correct per RN's own docs, and this is what real iOS/Android screen readers read), but don't reliably surface as `aria-selected`/`aria-expanded` in React Native Web's DOM output specifically — confirmed by inspecting the actual rendered HTML. This is a platform-specific gap in RN Web's ARIA mapping, not a bug in this fix; same category as this project's other already-disclosed web-only verification gaps (`Alert.alert` no-op on web, biometrics/notifications native-only). The role+accessible-name fix (the more important half) is confirmed working on web; full state-announcement behavior needs confirming on a real device with VoiceOver/TalkBack before launch.
  - Full suite green (41 mobile tests, 75 tax-engine tests, both typechecks clean) — this was a props-only change with no new logic to unit test.
- [x] **Finalize app identity before submission — DONE.** `apps/mobile/app.json` now has a real display name ("**SetAsideTracker**" — ties directly to the dashboard's "Set aside for taxes" feature; **not yet verified for App Store name availability/trademark conflicts**, confirm before final submission), slug (`setasidetracker`), `ios.bundleIdentifier`/`android.package` (`com.gigtaxtracker.app`, shared across both platforms), and initial build numbers. Internal project/package naming (`@gig-tax-tracker/tax-engine`, repo folder names) deliberately left unchanged — only the user-facing app identity changed.
- [x] **Project version control + CI set up.** The project had no git repository at all until now — initialized one (root commit covers everything built so far) since Codemagic (and any CI) requires a connected GitHub/GitLab/Bitbucket repo. Added `codemagic.yaml` with an `ios-testflight` workflow: install monorepo deps → build + test `services/tax-engine` → typecheck/test `apps/mobile` → `expo prebuild --platform ios` (Continuous Native Generation — `ios/`/`android/` are gitignored and regenerated fresh every build from `app.json`, never hand-edited or committed) → CocoaPods install → automatic code signing via the existing App Store Connect integration → build the `.ipa` → publish to TestFlight.
  - **Updated to mirror the proven working config from an existing app on the same Codemagic account**, rather than a guessed-at integration setup: reuses the `AppleConnect` variable group (App Store Connect API key creds are Apple Developer Team-level, not per-app, so the same group works across apps) + the `keychain initialize` / `keychain add-certificates` / `xcode-project use-profiles` signing sequence + raw `api_key`/`key_id`/`issuer_id` publishing, instead of the originally-guessed `integrations.app_store_connect:` block. Also added automatic build-number incrementing via `agvtool` + Codemagic's built-in `$CM_BUILD_NUMBER` (App Store Connect requires a strictly increasing build number per upload).
  - **Update:** pushed to `https://github.com/jsnyde03/setAsideTracker` and connected in Codemagic (project type: React Native App — Codemagic uses the committed `codemagic.yaml` directly rather than its own generated workflow once one exists). The `com.gigtaxtracker.app` bundle ID / App Store Connect app record already exists — it showed up selectable in Codemagic's signing UI, confirming that earlier "needs registering" caveat is resolved. One naming mismatch to clean up when convenient (cosmetic, not blocking): the App Store Connect app record is still named "SetAside" from before the rename to "SetAsideTracker" — bundle ID is the only thing that has to match technically, so this can be fixed anytime directly in App Store Connect's app listing.
  - **Resolved:** confirmed directly against this Codemagic account — the `AppleConnect` variable group name and CI config are correct, no longer just a reference borrowed from another app.
  - **Real CI bug found and fixed (caught from an actual failed build, not local testing) — two contributing issues, found across two diagnostic passes.** A build failed with `Failed to set code signing settings for node_modules/@react-native-community/datetimepicker/ios/RNDateTimePicker.xcodeproj`, and the user correctly identified it as a regression introduced specifically by the large multi-feature push, not a pre-existing flake.
    - **First fix (real, but not the actual root cause):** `codemagic.yaml` had both the modern automatic `ios_signing` config (under `environment`) *and* the older manual `keychain initialize`/`keychain add-certificates` steps — confirmed against Codemagic's own docs that those are redundant once `ios_signing` is configured, since Codemagic already auto-initializes its keychain and fetches the matching certificate before any script runs. Removed both; kept `xcode-project use-profiles`.
    - **Actual confirmed root cause, found on the next failure:** `xcode-project use-profiles`'s `--project` flag defaults to the glob `**/*.xcodeproj`, matching *every* Xcode project in the repo — including ones bundled inside third-party native module source under `node_modules`. `@react-native-community/datetimepicker` (added earlier in that same large push) was the first dependency in this project to ship its own standalone `.xcodeproj`, which has no provisioning profile of its own to match — that's what the failure was actually about, and explains exactly why it only started happening after that push specifically. Fixed by scoping `--project` explicitly to the real app project (`ios/SetAsideTracker.xcodeproj`) and adding `--warn-only` as a safety net against any future dependency doing the same thing.
    - **Third issue, found on the build *after* that one (signing now passed, archive then failed):** the IPA build itself failed. `@sentry/react-native`'s Expo config plugin (also added in the same large push) injects an "Upload Debug Symbols to Sentry" Xcode build phase that runs `sentry-cli` during archive — confirmed via the actual `@sentry/react-native` GitHub issue tracker as a known failure mode when no real Sentry project/DSN/org/auth-token is configured (exactly this app's situation, since Sentry is deliberately scaffolded-but-not-live — see the crash-reporting item above). That upload step has nothing to authenticate against and fails with a nonzero exit code, aborting the whole archive. Fixed with `SENTRY_DISABLE_AUTO_UPLOAD: "true"` under `codemagic.yaml`'s `environment.vars` — matches the "not live yet" intent directly rather than letting an inactive feature fail the build. Remove this var once a real Sentry project/DSN/auth-token exists and source-map upload is actually wanted.
  - **Android intentionally not included** — no Google Play Console signing credentials set up yet. Adding it later follows the identical shape (prebuild → Gradle → `google_play` publishing) once those exist.
  - Verified everything that's verifiable outside macOS: `npm ci` at the repo root, `services/tax-engine`'s build + full test suite, and `apps/mobile`'s typecheck + full test suite all pass cleanly post-clean-install. `expo prebuild --platform ios` itself can only run on macOS/Linux (Expo skips iOS native generation on Windows) — confirmed the config is otherwise valid by successfully running `expo prebuild --platform android` instead as a structural sanity check (same `app.json`, same code-generation pipeline), then discarded that test output since Android isn't part of this CI workflow yet.
- [x] **True hourly rate calculator — DONE, pulled forward from v1.4 (Retention & Growth Features).** New optional `Entry.hoursWorked` field (`apps/mobile/src/types.ts`) plus a matching "Hours worked (optional)" `TextField` in `AddEntryScreen`, satisfying the prerequisite the original gap called out. New pure `effectiveHourlyRate()` in `calculations.ts`: take-home pay (gross earnings minus logged cash expenses minus the tax set-aside) divided by total hours worked across the year's entries, returning `undefined` when no hours have been logged anywhere (rather than dividing by zero) so the dashboard line only appears once it's meaningful. `EntryAggregate` gained `totalHoursWorked` (entries without `hoursWorked` contribute 0, not an error). New "Effective hourly rate (after taxes)" line in the existing "Total earnings logged" card on `DashboardScreen`.
  - **Deliberate simplification, flagged in code:** does NOT additionally subtract a mileage-implied vehicle-cost dollar estimate on top of the tax set-aside, even though the original gap description hedged that as "ideally" included — the standard mileage rate already reduces the tax set-aside via the tax engine's deduction, so subtracting it again here would double-count it as if it were a second real cash outflow, not just a tax deduction.
  - 4 new tests (3 for `effectiveHourlyRate`: no-hours-logged returns undefined, the standard divide-by-hours case, and the rare negative-rate case where the tax set-aside exceeds take-home pay; 1 for `aggregateEntries` summing `hoursWorked` across entries that have it set while treating entries without it as 0) — 45 mobile tests passing. Tax-engine untouched.
  - Verified end-to-end in a real browser session: an entry with no hours logged correctly showed no hourly-rate line at all; adding a second $300 entry with 10 hours logged (total $800 earnings, $113.04 tax set-aside, $0 expenses across both) showed "$68.70/hr," matching hand-checked arithmetic exactly ((800 − 0 − 113.04) ÷ 10 = 68.696).
- [x] **CSV export — DONE.** New "Export Data (CSV)" row in `SettingsScreen`, under a "Data" section alongside "Clear All Data." Pure, platform-agnostic `entriesToCsv()` (`apps/mobile/src/csvExport.ts`) builds RFC-4180-style CSV text (one row per entry, sorted oldest-first — the natural order for a tax-time export, unlike the dashboard's newest-first list) covering every `Entry` field including the new `hoursWorked`. The actual save/share mechanics are platform-split, same pattern as `DateField`: `exportEntriesAsCsv.ts` (native — writes to the cache directory via the new `expo-file-system` `File`/`Paths` API confirmed against the Expo v56 docs, since SDK 56 deprecated the old `writeAsStringAsync()` style, then opens the system share sheet via `expo-sharing`) and `exportEntriesAsCsv.web.ts` (browser — a Blob + temporary anchor element, the standard download pattern with no native-module equivalent at all). Both new Expo packages installed via `npx expo install`.
  - 3 new tests for `entriesToCsv` (header + sorted rows, hours-worked blank-vs-set, empty-list edge case) — 48 mobile tests passing. Tax-engine untouched.
  - Verified end-to-end in a real browser session: triggering the export produced a real file download (`entries-2026-06-26.csv`) whose actual content was read back and confirmed correct against the logged entry.
  - **Not verified by browser testing, by necessity:** the native file-write + share-sheet path (`exportEntriesAsCsv.ts`) needs a real device/simulator — same disclosed-gap category as this project's other native-only paths (the date picker's native side, biometrics, notifications).
- [x] **Disclaimer flows — DONE.** A passive disclaimer already existed in both required spots from earlier work — `OnboardingScreen` and a persistent footer on `DashboardScreen` ("Estimates for planning purposes only — not tax advice.") — but ROADMAP §2.3 specifically calls for the disclaimer to be **explicit**, and passive footer text doesn't meet that bar for a financial app's liability coverage. Upgraded onboarding's into a real acknowledgment: a checkbox (`accessibilityRole="checkbox"`) the user must tap before "Continue" proceeds, following the same `Alert.alert`-based validation pattern already used for every other required onboarding field. Left the Dashboard's persistent footer as plain text — that one already satisfies "persistently visible near tax figures" literally, and a checkbox makes no sense for a footer the user doesn't actively progress past.
  - No new unit tests (this is React Native UI/control-flow wiring with no new pure logic, same precedent as other UI-only changes like the date picker).
  - Verified end-to-end in a real browser session: attempting to continue past onboarding with the box unchecked correctly stayed on the onboarding screen; checking it and retrying correctly reached the dashboard.
  - **Resolved (decided during submission prep): using Apple's Standard EULA instead of a custom ToS.** App Store Connect provides a default "Standard Apple End User License Agreement" any app can opt into without drafting custom legal text — sufficient for v1.0's generic terms (no warranty, no redistribution, etc.), since the app-specific liability concern (tax estimates aren't advice) is already covered by the explicit onboarding checkbox + persistent dashboard disclaimer above, not by a ToS. Re-visit if Android/Play Store ships later — Google Play has no equivalent built-in default, so a real ToS would be needed at that point.
- [x] **App Store / Play Store submission prep — DONE (everything code/asset/copy-side; remaining steps are manual App Store Connect/Play Console actions, assumed to get done outside this process rather than tracked as open engineering items).**
  - **Real app icon shipped, replacing Expo's default placeholder.** The previous `icon.png`/`android-icon-*.png`/`favicon.png`/`splash-icon.png` were still Expo's generic template asset (a blue chevron with construction guides) — would have looked completely unprofessional, and likely drawn extra scrutiny in a financial-app review. Generated a simple branded icon (shield + dollar sign, in the app's existing primary-blue gradient + gold accent — the same color pairing already used for the dashboard's "set aside" shield-checkmark icon) using Playwright to render SVG/HTML to PNG at each required size, since no image-editing tool (ImageMagick/sharp/PIL) was available in this environment — confirmed this is a real, reusable technique, not a one-off hack. Verified each transparent layer (Android adaptive icon foreground/monochrome) by compositing over a colored background before finalizing, since transparency is invisible against a white preview pane otherwise.
  - **New `PRIVACY_POLICY.md`** (repo root): drafted to accurately reflect this app's actual behavior — local-only storage, no backend, no account system, biometric auth handled entirely by the OS, crash reporting/analytics present in code but not currently active (ties to the earlier scaffolded-not-live state), user-initiated CSV/backup exports being outside the app's control once shared. Placeholders (last-updated date, support email) and hosting are the user's to finish.
  - **New `STORE_LISTING.md`** (repo root): app description, subtitle, keywords, and promotional text, drafted from the actual shipped v1.0 feature set (deliberately excludes W2 income tracking, since that's disabled — see this section's earlier follow-up note).
  - **New `APP_REVIEW_NOTES.md`** (repo root): explains to human reviewers that there's no login/account system at all (by design), with step-by-step instructions for testing the core flow without test credentials, plus a note addressing the likely "is this financial advice / does this move money" review concern directly.
  - **New `SCREENSHOT_PLAN.md`** (repo root): which screens to capture and in what order (dashboard hero shot leading, then catch-up status, logging flow, entries list, backup/security/dark-mode settings — 7 total), what sample data to set up first (a believable multi-platform persona in a state with income tax, set-aside status in the reassuring "on track" state rather than the scary "behind" one), and confirmed-current exact pixel sizes for both stores (Apple's 1320×2868 6.9"/1290×2796 6.7"; Google Play's 1080×1920 plus the separately-required 1024×500 feature graphic).
  - **Resolved: `app.json`'s `ios.supportsTablet` changed from `true` to `false`.** The UI has never been designed or tested for iPad layouts (single-column phone layout that would stretch awkwardly), and leaving it `true` would have required iPad screenshots for a tablet experience that doesn't actually work well — going iPhone-only for v1.0 avoids that mismatch entirely rather than just working around it at the screenshot-submission level. Re-enabling later is a one-line change, but would need real tablet-layout design work first.
  - **AppleConnect Codemagic variable group confirmed correct** directly against the account (no longer just a borrowed reference from another app).
  - **Remaining steps are the user's, by nature of being manual App Store Connect/Play Console actions** (creating the actual listing, hosting the privacy policy, taking device screenshots, triggering the TestFlight build via the existing `codemagic.yaml`, and submitting for review) — not re-listed here as open items, since none of them are engineering work. Android/Play Store submission is out of scope for v1.0 entirely — no Play Console credentials exist yet, consistent with the CI setup work's "Android intentionally not included" note; revisit as its own future item if/when Android ships.
- [x] **Crash reporting + basic analytics instrumentation — SCAFFOLDED, not yet live.** No Sentry account/DSN exists yet for this app (confirmed with you before building — deliberately scaffold-only, same "ready to wire up, not yet pretending to be live" pattern as the `AppleConnect` Codemagic variable group), so this wires up the integration shape without it actually reporting anywhere yet.
  - **Crash reporting:** `npx expo install @sentry/react-native` (the officially Expo-supported SDK), with only its minimal native-config plugin string added to `app.json` — deliberately did NOT add the `@sentry/react-native/expo` plugin's org/project/auth-token config block, since that triggers build-time source-map upload and misconfiguring it with placeholder credentials could break the already-working Codemagic build. New `apps/mobile/src/errorReporting.ts`: reads `EXPO_PUBLIC_SENTRY_DSN` (Expo's convention for client-readable env vars — safe to be public, a DSN is a write-only ingestion endpoint, not a secret) and only calls `Sentry.init()` if it's actually set; `reportError()` always logs to the console (useful in dev regardless) and additionally calls `Sentry.captureException()` once a real DSN exists. Wired into the existing `ErrorBoundary`'s new `componentDidCatch` (previously only had `getDerivedStateFromError`, which updates render state but isn't the right lifecycle hook for side effects like reporting) and into every one of `App.tsx`'s 8 existing try/catch blocks, alongside their existing `Alert.alert` calls — these were real swallowed-then-alerted errors that are worth seeing in Sentry too once it's live, not just new code.
  - **Analytics:** new vendor-agnostic `apps/mobile/src/analytics.ts` — no vendor (PostHog/Amplitude/etc.) has been picked yet either, so `trackEvent()` just logs to the console in dev and no-ops in production for now. Wired at two meaningful points (`onboarding_completed` with state/hasW2Job, `entry_logged`/`entry_updated` with platform) to prove out the call-site pattern; swapping in a real backend later only requires changing this one file, no call-site changes.
  - **Found and fixed a real bundling break while verifying this, not a code bug:** installing `@sentry/react-native` initially broke the web bundle entirely ("Unable to resolve @sentry/core" despite the file existing on disk) — root cause was Metro's module-resolution cache being stale after several native-module installs in the same session without a full cache-clearing restart (`expo start --web --clear` resolved it immediately, and the file really was present and correctly exported). Worth remembering: a "file doesn't exist" Metro resolution error after an `expo install` is itself a `--clear` signal before assuming the package is actually broken.
  - No new unit tests (this is wiring/side-effect code with an intentionally inert no-op default, not new pure logic). Full suite green (48 mobile tests, 75 tax-engine tests, both typechecks clean).
  - Verified end-to-end in a real browser session, post-cache-clear: completing onboarding and logging an entry produced the expected `[analytics] onboarding_completed {...}` and `[analytics] entry_logged {...}` console output with correct properties, and the bundle compiled with zero errors.
  - **Still needed before this is actually live:** a real Sentry project + DSN (set `EXPO_PUBLIC_SENTRY_DSN`, and separately add the org/project/auth-token plugin config for source maps), and a decision on which analytics vendor to wire `analytics.ts` to.
- [x] **Account recovery / multi-device basics — DONE, lightweight/no-backend approach (confirmed with you before building).** ROADMAP §8.3 explicitly flags this as implying a real backend (auth + cloud storage), which this app deliberately doesn't have. Rather than building that, this is satisfied by combining (a) the OS's own device-backup inclusion — AsyncStorage data isn't excluded from iCloud/Android auto-backup by anything in this app's config, so a device restore already recovers it for free — and (b) the new manual export/import backup feature below, which is also the literal "Local backup/restore" item from later in this same list; both close together as one feature, not duplicated work. Recovering to a *new* device specifically requires the manual export/import step, since there's no account to automatically re-associate a fresh install with.
- [x] **Dark mode — DONE.** Turned out NOT to be the "cheap, just add a token set" lift this item's original description assumed — confirmed with you before building, since every screen's styles were built via a module-level `StyleSheet.create({...})` computed once at import time with colors baked directly in, so a runtime toggle wouldn't have re-rendered anything without a real refactor. Did the full refactor across all ~14 affected files (not the smaller partial option) since a half-themed app would have been worse than not shipping it.
  - `apps/mobile/src/theme.ts`: split into `lightColors`/`darkColors` (identical key shape) plus a `Colors` type; the old static `colors` export was removed entirely (not kept as a "deprecated but still there" shim) so the compiler would surface every remaining un-migrated usage as a hard error — used that error list as the literal migration checklist.
  - New `apps/mobile/src/ThemeContext.tsx`: `ThemeProvider` resolves a saved `"light" | "dark" | "system"` preference against `useColorScheme()` (only relevant when the preference is `"system"`) and provides `{ colors, isDark }`; `useTheme()` for function components, plus the raw `ThemeContext` export for `ErrorBoundary` (a class component, which can't call hooks — uses `static contextType` instead, with a typed getter rather than a `declare context: ...` field, see the bundling note below).
  - New `AppSettings.colorScheme` field, defaulting to `"system"` when absent; persists through the existing `saveAppSettings`/`getAppSettings`, no new storage key. New "Appearance" section in `SettingsScreen` (System/Light/Dark `Chip` row).
  - Mechanical per-file change, identical in every one of the ~14 components/screens: remove the static `colors` import, add `const { colors } = useTheme();`, and turn the module-level `const styles = StyleSheet.create({...})` into a `function createStyles(colors: Colors) { return StyleSheet.create({...}); }` called inside the component. Zero changes to the actual style *values* — purely moving when/how they're computed.
  - `app.json`'s `userInterfaceStyle` changed from the hardcoded `"light"` to `"automatic"`, and `App.tsx`'s `StatusBar` now reads `isDark` to pick light/dark status bar icon color.
  - **Found and fixed a real bundling break while verifying this, not a typecheck-only issue:** `tsc --noEmit` accepted `ErrorBoundary`'s `declare context: React.ContextType<typeof ThemeContext>;` field cleanly, but Metro's Babel transform doesn't support TypeScript's `declare` class-field modifier and failed the actual web bundle with a syntax error — `tsc` and the real bundler disagree on what's valid here. Fixed by using a `private get theme()` getter instead of a declared field. Worth remembering: a clean `tsc` typecheck does not guarantee Metro can actually bundle a file: a real `expo start --web` bundle (not just `tsc`) is the only thing that catches this category of bug.
  - No new unit tests (this was styling/theming wiring, no new pure logic). Full suite green (54 mobile tests, 75 tax-engine tests, both typechecks clean).
  - Verified end-to-end in a real browser session across Settings, Dashboard, and AddEntry: switching the Appearance selector to "Dark" correctly re-themed every visible surface (cards, inputs, chips, buttons, status colors) on all three screens, with the selected "Dark" chip itself correctly highlighted.
  - **Not changed, deliberately:** the dark "Set aside for taxes" gradient card's own internal hardcoded colors (`#1F2937`/`#111827` gradient, white/light-gray text) — that card was already dark-styled by original design intent (a fixed-dark "premium fintech" accent card), independent of the overall app theme, so it looks correct unchanged in both light and dark mode.
- [x] **Local backup/restore (export/import a JSON data snapshot) — DONE.** New "Backup & Restore" section in `SettingsScreen`: "Create Backup File" and "Restore from Backup." Pure `buildBackupSnapshot()`/`parseBackupSnapshot()` (`apps/mobile/src/backup.ts`) handle the versioned JSON shape (`{ version, exportedAt, localUserProfile, taxProfile, entries, appSettings }`) and validation — `parseBackupSnapshot` throws a descriptive error (shown via `Alert.alert`, not a cryptic parse failure) for invalid JSON, a non-object payload, an unsupported version, or a missing entries array, since restoring is destructive (it overwrites everything currently on the device) and deserves a clearer failure than a typical best-effort parse. New `exportBackupSnapshot()`/`restoreBackupSnapshot()` in `repository.ts` do the actual AsyncStorage reads/writes (restore replaces the entries list wholesale via a new internal `saveEntries`, not a merge — a backup file IS the new source of truth). The file save/pick mechanics are platform-split exactly like the CSV export and date picker: `backupFile.ts` (native — `expo-file-system`'s `File`/`Paths` + `expo-sharing` for export, the newly-installed `expo-document-picker` + `File.text()` for import) and `backupFile.web.ts` (browser — a Blob/anchor download for export, a hidden `<input type="file">` for import). Restoring requires explicit confirmation via `Alert.alert` before it proceeds (same destructive-action pattern as "Clear All Data"), and `App.tsx`'s new `handleRestoreBackup()` updates all in-memory state and routes to dashboard/onboarding afterward without requiring a full app restart.
  - 6 new tests in `backup.test.ts` (full round-trip with real data, round-trip with a never-onboarded/empty snapshot, and 4 validation-failure cases) — 54 mobile tests passing. Tax-engine untouched.
  - **Found and worked around a real, disclosed limitation while verifying this — not a bug in the feature:** the restore confirmation is gated behind `Alert.alert`, which is a no-op on React Native Web (same already-documented gap affecting "Clear All Data" and the entry-delete confirmation) — clicking "Restore from Backup" on web never opens the file picker at all, since the confirmation dialog's button callback never fires. Verified the underlying restore mechanics work correctly anyway by temporarily removing the `Alert.alert` wrapper (same technique used to verify the `ErrorBoundary` earlier), confirming a full export→restore round trip correctly returned the exact same data (a $777 entry survived intact) and landed back on the dashboard, then reverting the temporary change. The confirmation-dialog click-through itself still needs a real-device check before launch, like the other `Alert.alert`-gated flows.
  - Verified the export half directly on web (no bypass needed, since it doesn't go through `Alert.alert`): downloaded a real backup file and confirmed its JSON content matched the live app state exactly.
- [x] **Notifications on/off toggle — DONE.** Found during real-device testing: onboarding prompts for the OS notification permission to schedule quarterly due-date reminders, but there was no way to turn reminders back off afterward — a real miss for anyone who didn't want them. New `AppSettings.remindersEnabled?: boolean`, defaulting to `true` when absent (reminders were always-on before this setting existed, so absence of the field preserves existing behavior rather than silently turning them off for existing users). New `cancelQuarterlyReminders()` in `scheduleReminders.ts` (mirrors the existing `scheduleQuarterlyReminders()`, same web no-op). New "Notifications" section in `SettingsScreen` with a `Switch`, wired through `App.tsx`'s new `handleToggleReminders()` (persists via `saveAppSettings`, then calls `scheduleQuarterlyReminders()` or `cancelQuarterlyReminders()` depending on the new value). Both existing `scheduleQuarterlyReminders()` call sites (cold-start load effect, `handleOnboardingComplete`) are now gated behind the current `remindersEnabled` value instead of calling unconditionally. `handleToggleAppLock`/`handleChangeColorScheme`'s existing `saveAppSettings(...)` calls updated to also include `remindersEnabled`, since `saveAppSettings` is a full overwrite, not a merge.
  - No new unit tests (wiring/persistence, no new pure logic). Full suite green (54 mobile tests), typecheck clean.
  - Verified in a real browser session: completed onboarding, opened Settings, confirmed the new "Notifications" section renders below "Security" with the toggle on by default.
- [x] **Dashboard tax-year switcher — DONE.** Both 2025 and 2026 tax-year configs already existed in the tax-engine, and `computeTaxEstimate`/`entriesForYear`/`yearsWithEntries` already supported scoping to an arbitrary year, but `DashboardScreen` always called `computeTaxEstimate` with its default (today's calendar year) and had no UI to view a prior year's data — a real gap once entries existed across two tax years. New local `selectedYear` state in `DashboardScreen`, defaulting to the current calendar year. `availableYears` is `yearsWithEntries(entries)` plus the current calendar year always included (so a brand-new year is selectable even before any entry exists for it), deduped and sorted descending. The year badge next to "Your earnings" becomes a left/right chevron switcher whenever more than one year is available (a plain static badge otherwise, unchanged from before); chevrons are disabled (and visually dimmed) at the oldest/newest ends of the range rather than wrapping around. Every year-scoped figure on the screen — earnings total, the tax estimate breakdown, the catch-up-status card, `amountSetAsideByYear` — already read from `taxEstimate.year`/`entriesForYear(entries, year)`, so passing `selectedYear` into `computeTaxEstimate` as its third argument was the only logic change needed; no new pure functions required.
  - Deliberately not persisted across app restarts — this is a display preference for browsing history, not a setting, so it resets to the current year on cold start like a calendar app would, rather than needing a new `AppSettings` field.
  - No new unit tests (pure UI wiring over already-tested year-scoping functions). Full suite green (54 mobile tests), typecheck clean.
  - Verified in a real browser session: with only a 2026 entry, the badge shows as a plain static label (no switcher chrome). After logging a $500 entry dated 2025-03-15, the switcher appeared; clicking the back chevron correctly switched the entire dashboard — earnings ($0 → $500), the tax estimate ($0 → $70.65, recomputed against the 2025 config), and the catch-up status (recalculated against 2025's own `amountSetAsideByYear` entry, correctly separate from 2026's) — to 2025's data.
- [x] **[HIGH PRIORITY, BLOCKS LAUNCH] Rebuild W2 tax modeling — re-enable `W2_JOB_SUPPORT_ENABLED` with the real fix, not the v1.0-launch disable.** Pulled back into v1.0 (was briefly scoped as a separate v1.1) once it became clear there's no reason to ship the disabled flag to real users when nothing's been submitted yet and the fix is already fully designed. **DONE.** All three bugs fixed and the feature re-enabled.
  - **Pay-stub-based input model.** Replaced `w2PaycheckAmount` (labeled "take-home pay" but fed as gross) with four separate per-paycheck fields: `w2GrossPayPerPeriod` (top-line gross), `w2RetirementPerPeriod` (401k/403b — reduces income tax but NOT FICA), `w2PreTaxBenefitsPerPeriod` (insurance/HSA/FSA — reduces both income tax and FICA wages), and `w2PayFrequency` (unchanged). New `deriveW2Incomes()` in `calculations.ts` derives the two distinct annual figures the engine needs: `w2FicaWages` (gross − pretax benefits) and `w2FederalTaxableIncome` (gross − 401k − pretax benefits).
  - **YTD actuals path added.** New optional `TaxProfile` fields `w2YtdFederalWithheld` and `w2YtdStateWithheld` (from the YTD column of the most recent pay stub). When present, withholding credit = YTD actual already withheld + model estimate for remaining pay periods (annual estimate × (1 − elapsedFraction)). Without actuals, credit = full annual estimate (no elapsed-time proration of a hypothetical — the previous bug). Either path is correct; actuals are more precise.
  - **seTax.ts SS wage base + AMT threshold fix.** `calculateSeTax` now takes an optional `otherFicaWages` parameter. Available SS wage base = max(0, $176,100 − otherFicaWages); available AMT threshold = max(0, $200k/$250k/$125k − otherFicaWages). Tested with 5 new unit tests covering partial reduction, full exhaustion, and MFS threshold. `TaxEstimateInput` gained a matching `otherFicaWages?` field (separate from `otherTaxableIncome` since they differ when 401k is in play); `estimate.ts` passes `otherFicaWages ?? otherTaxableIncome` as a conservative default when not provided.
  - **UI updated.** Both `OnboardingScreen.tsx` and `EditTaxProfileScreen.tsx` now show the new pay-stub fields (gross, 401k optional, pretax benefits optional, YTD withheld federal/state optional, end date). The annual gross hint ("≈ $78,000/year gross") updates live from gross-per-paycheck × frequency. `W2_JOB_SUPPORT_ENABLED` set back to `true` in `featureFlags.ts`.
  - 5 new SE-tax unit tests + 6 new state-tax tests (from HoH/MFS work, landed in same session) + all mobile test fixtures updated. 88 tax-engine tests passing, 54 mobile tests passing. Both typechecks clean after rebuilding `dist/`.
  - **Verified end-to-end in browser (Playwright):** W2 toggle visible, all 7 new field labels rendered, gross pay → frequency → annual hint math correct ($3,000 × 26 = $78,000/year gross), form submits and reaches dashboard.
- [x] **[HIGH PRIORITY, BLOCKS LAUNCH] Add Head of Household and Married Filing Separately to `FilingStatus`** — flagged in [ROADMAP §8.1](ROADMAP.md#L217) as "worth fixing before/around v1 launch" but never made it into the original v1.0 punch list; caught during a post-launch-planning gap review. `services/tax-engine/src/types.ts`'s `FilingStatus` is still only `"single" | "marriedFilingJointly"` — every bracket table, standard deduction, and Additional Medicare threshold in `2025.ts`/`2026.ts` is keyed on just those two. Unlike the unsupported-state gap (which shows an honest warning), a Head of Household filer today would silently get run through the Single brackets with no warning at all — a real, undisclosed accuracy bug, not just a missing option (the Single brackets overstate tax owed relative to HoH's larger standard deduction/more favorable brackets, the safer direction of error, but still wrong and undisclosed). Head of Household is common among single-parent gig workers, a segment this app should not be quietly miscalculating for.
  - Scope: add `"headOfHousehold"` and `"marriedFilingSeparately"` to the `FilingStatus` union; add their bracket/standard-deduction/Additional-Medicare-threshold figures to both `2025.ts` and `2026.ts` (sourced and verified the same way the existing [TAX_CONFIG_REVIEW.md](TAX_CONFIG_REVIEW.md) process verifies other figures); add both options to `OnboardingScreen.tsx`/`EditTaxProfileScreen.tsx`'s filing-status `Chip` row (currently only renders `FILING_STATUS_OPTIONS` for Single/MFJ).
  - Child Tax Credit phase-out thresholds (`childTaxCredit.ts`) are also keyed by filing status — confirm HoH/MFS figures there too while this is open, not just the income-tax brackets.
  - **DONE.** `FilingStatus` extended to all 4 statuses. Federal brackets/deductions sourced from IRS Rev. Proc. 2025-32 (2026) and OBBBA-confirmed figures (2025). All 50 states + DC updated in both `taxYears/2025.ts` and `stateTaxConfigs/2026.ts` with per-state HoH/MFS deductions (state-specific where published: NY $11,200, GA $18,000, NC $19,125, CT $19,000, MA $6,800; federal-conforming for SC/DC; single used as default elsewhere) and brackets (NY has distinct HoH bracket thresholds; all other states use single as HoH/MFS approximation). Local tax tables (NYC, MD counties) updated. `SettingsScreen.tsx` label map extended. 8 new tests added; 83 passing. Both typechecks clean.
  - **Verified end-to-end in browser (Playwright):** All 4 filing-status chips visible and selectable on onboarding. HoH → CA and MFS → CO both navigate to dashboard with correct state tax line labels, no JS errors. The `$0.00` figures shown are correct (zero entries logged, and at $10k income HoH's $24,150 standard deduction fully offsets taxable income — the zero is right, not a bug).
**Ships to:** general public, free tier only. **Exit criteria met:** W2 rebuild complete (`W2_JOB_SUPPORT_ENABLED` re-enabled), all four filing statuses verified against IRS figures, full test suite green (88 tax-engine + 54 mobile tests). No payment infra live — validate retention before monetizing.

</details>

## 🔄 v1.1 — Premium Tier Launch + Free Tier Growth

<details>
<summary>Status: In Progress</summary>

**Status: In Progress** — work lives on the `v1.1` branch (not merged to `master` until v1.0 is live). Sequencing, dependencies, and external prerequisites are tracked in [V1.1_EXECUTION_PLAN.md](V1.1_EXECUTION_PLAN.md). Shipped so far: Workstream 0 (Playwright + Maestro + CI testing harness), the "Show your math" audit trail (Phase A #1), in-app tax literacy (Phase A #2), the What-if earnings simulator (Phase A #3), the platform earnings comparison (Phase A #4), the earnings share card (Phase A #5), and the well-timed app rating prompt (Phase A #6). **Phase A (the free-tier additions) is complete** — what remains in v1.1 is the premium track (Phase B), which is gated on Sentry/analytics + RevenueCat setup.
**Goal:** launch monetization once free-tier retention looks healthy; simultaneously add free features that deepen engagement and drive organic sharing.

**Prerequisite: wire up Sentry and analytics before this ships.** Both `apps/mobile/src/errorReporting.ts` and `analytics.ts` are scaffolded with no real backend (`EXPO_PUBLIC_SENTRY_DSN` unset, no analytics vendor configured). Set up a real Sentry project + DSN, and pick an analytics vendor (PostHog or Amplitude are natural fits) and wire `analytics.ts` to it. You need crash data and real usage patterns before making confident feature-prioritization decisions — this is operational work, not a separate version, and should happen immediately post–App Store approval.

### Free additions (ships alongside premium, never paywalled)

- ✅ **"Show your math" audit-trail view.** *(Shipped, `v1.1` branch.)* Each line in the dashboard's tax breakdown (SE tax, federal income tax, state tax + credit/local, child tax credit, W2 withholding credit) is now tappable and opens a bottom-sheet showing the actual calculation: AGI → standard deduction → taxable income → the per-bracket tax, plus the SS/Medicare split and credit details. Engine exposes this via additive `standardDeductionUsed`/`bracketsApplied` fields (`AppliedBracket`, `applyBracketsDetailed`); UI is `BreakdownDetailSheet` fed by the pure, reusable `breakdownDetails.ts` builder. Covered by engine unit tests, a mobile builder test, and a `show-your-math` Playwright flow. Never paywalled per [ROADMAP §9.4](ROADMAP.md) and the tier-gating principle — this is the trust layer that converts free users to subscribers.
- ✅ **"What-if" earnings simulator.** *(Shipped, `v1.1` branch.)* A dashboard "What if I earned more?" button opens `WhatIfScreen`, which pre-fills this year's actual totals and recomputes the projected tax set-aside and effective hourly rate live as the user edits earnings/expenses/miles/hours — then compares the projection to the real dashboard number. No AI cost — pure tax-engine reuse via `computeWhatIfEstimate`, which shares `estimateFromAggregate` (incl. the W2-withholding credit) with the dashboard. Pulled forward from old v1.4. Free per [ROADMAP §5](ROADMAP.md). Strong between-shifts engagement: "what if I take an extra Flex block today?"
- ✅ **In-app tax literacy.** *(Shipped, `v1.1` branch.)* Each "show your math" detail sheet now ends with a "What these mean" row of tappable term pills (SE tax, standard mileage rate, AGI, standard deduction, tax brackets, withholding, etc.) that expand a plain-language explainer inline — same surface as the audit trail. Backed by a pure `glossary.ts` (16 jargon-free definitions, enforced by a unit test) tagged onto each breakdown via `BreakdownDetail.terms`. Most users are first-time self-employed; this is the educational layer that makes the numbers feel trustworthy rather than magic. Per [ROADMAP §9.4](ROADMAP.md) and [§5](ROADMAP.md).
- ✅ **Platform earnings comparison.** *(Shipped, `v1.1` branch.)* A dashboard "Compare your platforms" card (shown once 2+ platforms have entries) opens `PlatformComparisonScreen` — per-platform total earnings, after-expense hourly rate, and entry count ranked with proportional bars, derived from existing `Entry.platform`/`hoursWorked` via `comparePlatforms()`. Organic sharing driver: "my DoorDash rate vs. Uber Eats." New idea, not yet in the roadmap.
- ✅ **Earnings share card.** *(Shipped, `v1.1` branch.)* A dashboard share button opens `ShareEarningsModal` previewing a branded `ShareCard` (earnings, set-aside, effective hourly rate, top platform), captured via `react-native-view-shot` and handed to the native share sheet (`expo-sharing`). Capture + share are iOS-only (gated off web); web preview is covered by a Playwright flow. High viral potential for Reddit/Facebook gig-worker communities. New idea, not yet in the roadmap.
- ✅ **App rating prompt (well-timed).** *(Shipped, `v1.1` branch.)* After a user logs their 5th entry (or meets their first catch-up), `maybeRequestReview` surfaces `expo-store-review` / `SKStoreReviewController` once. Pure trigger policy (`appReviewPolicy.shouldRequestReview`) is unit-tested; the prompt never fires on first launch. Crucial for App Store search ranking.

### Premium features (IAP via RevenueCat)

> Sequencing, prerequisites, code-audit, and testing strategy for everything below: **[PHASE_B_EXECUTION_PLAN.md](PHASE_B_EXECUTION_PLAN.md)**.

- **Payment infrastructure.** Apple IAP via RevenueCat (`react-native-purchases` SDK). Entitlement check at cold start; offline fallback must trust RevenueCat's locally cached entitlement rather than blocking premium features on a failed network call — this is the first real network dependency in an otherwise local-first app. Already decided over Stripe web checkout (App Review Guideline 3.1.1 requires IAP for in-app digital feature unlocking). Already enrolled in Apple Small Business Program (15% rate). RevenueCat also unifies with Google Play Billing once Android ships (v1.2) via the same SDK — no second integration. Per [ROADMAP §8.4](ROADMAP.md).
- 🔄 **PDF tax-ready summary export.** *(Built on `v1.1`, pending TestFlight verification.)* Premium-gated "Tax Summary (PDF)" in Settings renders a tax-ready PDF via `expo-print` ([taxSummaryPdf.ts](apps/mobile/src/taxSummaryPdf.ts) + web variant): the Schedule C breakdown, estimated tax, and amount-to-set-aside, from a pure, unit-tested HTML builder ([taxSummaryHtml.ts](apps/mobile/src/taxSummaryHtml.ts)) reconciled to the engine's `totalEstimatedTax`. Non-subscribers route to the paywall. Native print/share is the one path needing a device pass. Natural premium anchor, especially in filing season.
- 🔄 **Schedule C category alignment.** *(Built on `v1.1`.)* The four expense buckets + mileage are mapped to Schedule C lines in the pure, unit-tested [scheduleC.ts](apps/mobile/src/scheduleC.ts): Line 9 car/truck (standard mileage deduction + parking + tolls), Line 22 supplies, Line 25 utilities (phone). Line 13 depreciation / Line 17 insurance await custom categories (no tracked data yet). Drives the PDF export above. Per [ROADMAP §8.1](ROADMAP.md).
- **W-4 withholding optimizer.** For the W2+1099 combo: given the user's paycheck details (already modeled in the rebuilt W2 engine), suggest the specific additional per-paycheck withholding amount to enter on a new W-4 so the employer's withholding covers the 1099 tax liability — eliminating quarterly estimated payments entirely for many users. Pulled forward from v1.6 since the W2 rebuild just shipped and this is the natural premium follow-up from the same infrastructure. Genuinely non-obvious, high-value move most users don't know is available. Per [ROADMAP §9.2](ROADMAP.md).
- **Safe-harbor / Form 2210 underpayment-penalty calculator.** Surfaces the IRS's 110%-of-prior-year safe-harbor rule in the UI: "to avoid an underpayment penalty this year, you need to pay at least $X total." Requires user to input prior-year tax paid, or auto-pulls from the app's own 2025 estimate once data exists. Pulled forward from v1.5 — relevant year-round, not just filing season, and a real differentiator vs. mileage-tracker apps that never get here. Per [ROADMAP §2.2/§9.4](ROADMAP.md).
- **IRS-compliant mileage log fields.** Per-entry optional fields for business purpose and start/end location, making the standard mileage deduction audit-defensible (the IRS expects a contemporaneous log, not just a total). Per [ROADMAP §2.2](ROADMAP.md). Also the data-model prerequisite for GPS-assisted mileage (v1.3) — better to add it now via manual entry than redesign the schema later.
- **Custom/user-defined expense categories.** Beyond the existing parking/tolls/supplies/phone buckets. Per [ROADMAP §5](ROADMAP.md).
- **Year-over-year insights.** Meaningful only once the user has 2+ tax years of data — soft-gate the card until applicable rather than showing a near-empty screen on first subscribe.
- **Multi-state support.** See scoping note below.

**Multi-state scoping note:** Gig income is generally sourced to the state where the work is physically performed, not the worker's residence; some state pairs have reciprocity agreements; `TaxProfile` has one `state` field and the engine has no income-apportionment concept. Needs a design pass on (a) which state pairs to model first, (b) whether `Entry` needs a per-entry "state worked in" field vs. a resident/non-resident split, and (c) how to handle state credits for tax paid to a non-resident state — before estimating the build. Don't treat this as a quick add when premium scoping starts.

**Ships to:** general public. **Exit criteria:** premium conversion funnel works end-to-end (subscribe → features unlock → cancel/restore-purchases); Sentry and analytics live and reporting real data before this ships.

</details>

## ⬜ v1.2 — Android Launch + Retention & Growth

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** reach the majority of the gig-worker market (Android-majority demographics) and ship the retention/viral-growth features while the install base is young enough that growth mechanics compound best.

**Why Android belongs here, not later:** gig workers skew heavily toward Android — especially the lower-income, high-frequency drivers this app targets. Deferring Android cedes the majority of the addressable market to a competitor who ships sooner. RevenueCat already handles Google Play Billing via the same `react-native-purchases` SDK (no second integration), and the app has no backend; the marginal work is Play Console setup, Android CI, and screenshots. Android CI and store listing work can begin in parallel with v1.1 development.

### Android launch
- **Google Play Console setup.** Create app record for `com.gigtaxtracker.app`, upload a signed AAB, configure Play Console signing (upload key), complete the data-safety form (local-only storage, no backend, biometric auth handled entirely by the OS), draft store listing and screenshots.
- **Android CI in `codemagic.yaml`.** Add an `android-play-store` workflow: `npm ci` → `expo prebuild --platform android` → Gradle assemble → `google_play` publishing step. The shape mirrors the existing `ios-testflight` workflow and was already noted in the current config's design notes.
- **RevenueCat Play Billing.** Configure the Google Play product/entitlement in the RevenueCat dashboard; same `Purchases.configure()` + `getCustomerInfo()` call in the app already supports both stores via the SDK.
- **Android store assets.** Screenshots (1080×1920 + the separately-required 1024×500 feature graphic per existing `SCREENSHOT_PLAN.md`); verify the adaptive-icon foreground/background layers export correctly on an actual Android device.
- **Native-only verification gap — close it here.** This version is the forcing function to actually confirm all the accumulated "needs a real device" items that have been deferred since v0.3: `Alert.alert` confirmation dialogs (delete entry, restore backup, clear all data), the native `DateField.tsx` picker, biometric lock, notification delivery, the CSV and backup native share/file paths. One real-device test pass closes all of them for both platforms.

### Retention & growth features
- **Home-screen / lock-screen widget.** iOS WidgetKit + Android App Widget showing today's earnings and the running "set aside" total — glanceable without opening the app. Near-zero battery impact. Strong daily-retention driver; reminds users to log every shift. Per [ROADMAP §9.1](ROADMAP.md). Pulled forward from old v1.4.
- **Set-aside view customization (free) — CANDIDATE, placement TBD.** Let the headline "set aside" number be viewed by **cadence — daily / weekly / monthly** (spreading the *remaining* amount to set aside across the time left until the next due date), plus a per-dollar / per-shift rate, and an optional **per-platform allocation** (a proportional split of the total set-aside by each platform's share of net profit — *clearly labeled as an allocation*, since tax is progressive on total income, not platform-specific). Mostly a presentation layer over existing math (`computeCatchUpStatus`'s weekly amount, `effectiveSetAsideRate`) and the free platform-comparison view. Stays **free** per the tier-gating principle — it's the core daily-use loop and trust layer, not filing-season/power tooling. Pairs with the home-screen widget above. **Revisit placement after the v1.1 premium work** to confirm whether it lands here in v1.2 or as a fast-follow free addition. New idea (user, 2026-06-30), not yet in the roadmap.
- **Voice / hands-free logging.** Siri Shortcuts (iOS) and Google Assistant: "Hey Siri, log $45 from DoorDash" → pre-filled entry screen. Solves the core friction (driving, can't type) better than any UI polish. Per [ROADMAP §9.1](ROADMAP.md). Pulled forward from old v1.4.
- **Milestone celebrations + logging streaks.** Animation/confetti on "you've logged $10k this year," "$1k set aside," first quarterly deadline met, N-day logging streaks. Include a grace period (one missed day doesn't kill a streak). Per [ROADMAP §9.5](ROADMAP.md). Pulled forward from old v1.4.
- **Referral program.** Unique referral link, install attribution, small subscriber incentive (e.g. one free premium week). Per [ROADMAP §9.5](ROADMAP.md). Highest-efficiency growth lever at this stage.
- **Push notification expansion.** Beyond the existing quarterly due-date reminders: (a) catch-up warning when the gap between owed and set-aside exceeds a configurable threshold ("you're $500 behind — time to catch up"); (b) milestone notifications; (c) streak-break nudge. All gated on `remindersEnabled`.
- **Shift/earnings optimizer.** Analyzes user's own historical data to surface patterns: best time-of-day, day-of-week, platform combinations by earnings and effective hourly rate. Premium. Soft-gated until the user has 30+ entries across at least 3 weeks (otherwise there's no meaningful pattern). Per [ROADMAP §9.1](ROADMAP.md).

**Ships to:** general public (Android as a new-to-this-app audience). **Exit criteria:** Android build live on Play Store; all native-only verification gaps confirmed on real Android and iOS devices.

</details>

## ⬜ v1.3 — Mileage & Receipts Automation

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** automate the two most tedious manual inputs.
*(Pulled ahead of Platform Auto-Sync — higher per-user daily retention value, no backend infrastructure dependency. The IRS-compliant mileage log fields added in v1.1 provide the data-model foundation.)*

- **GPS-assisted mileage tracking.** Geofencing / significant-change location APIs (not continuous polling — battery concern per [ROADMAP §8.3](ROADMAP.md)). User confirms/rejects auto-detected trips rather than auto-logging everything.
- **Receipt photo capture + OCR for expenses.** Per [ROADMAP §9](ROADMAP.md).
- **Receipt / document vault.** Even before OCR, let users attach a photo to any entry — stored locally, no backend. The mileage log fields (v1.1) + a photo attachment is already a complete audit defense; OCR makes it faster. New idea, not yet in the roadmap.
- **IRS mileage log export.** The per-entry business purpose and location fields (v1.1) + this export = a formatted mileage log matching what an IRS auditor expects. Closes [ROADMAP §2.2](ROADMAP.md)'s "mileage substantiation" item end-to-end.

**Ships to:** general public, premium feature. **Exit criteria:** GPS trip detection runs passively on a real device through a full shift without noticeable battery drain.

</details>

## ⬜ v1.4 — Platform Auto-Sync

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** kill the manual-entry tax for the platforms that matter most.
*(Previously v1.2 — pushed after v1.3 since it requires the first real backend infrastructure, involves higher integration complexity, and must follow stable v1.1 payment infra.)*

- **Argyle (or equivalent) integration** for Amazon Flex, Uber, DoorDash, Instacart. Per [ROADMAP §3.5](ROADMAP.md). Verify Spark coverage before committing; email/statement parsing (Option B from the roadmap) is the fallback for any platform the aggregator doesn't cover.
- **First real backend infrastructure.** Argyle integration requires server-side webhook handling and token storage — this is when the app stops being purely local-first. Plan data model and compliance posture (encryption in transit/at rest, data retention, breach response plan per [ROADMAP §8.2](ROADMAP.md)) before starting.
- Premium-gated "auto-sync your earnings."
- **Sync-failure / offline handling.** Queue/retry rather than silent drop; manual entry must always work as a fallback. Auto-sync should never become a single point of failure for the core "know what to set aside" use case. Per [ROADMAP §8.3](ROADMAP.md).

**Don't start before v1.1 (payment infra) is stable** — auto-sync is the single best premium conversion driver and shouldn't debut into a broken checkout flow.

**Ships to:** general public, premium. **Exit criteria:** auto-synced entries match manual entries in accuracy during a parallel-run validation period.

</details>

## ⬜ v1.5 — Filing Season Toolkit

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** be indispensable in Jan–April, when gig workers actually file. **Date-sensitive — target shipping by early January regardless of where v1.3/v1.4 stand.**

*(Schedule C category alignment, safe-harbor calculator, and PDF export were pulled into v1.1 where they have year-round impact. What remains is specifically filing-season-timed.)*

- **1099-NEC / 1099-K reconciliation.** Let users check their tracked earnings total against the actual 1099 they receive from each platform. Catches missed entries; the single most trust-building feature at the most stressful moment of the gig-worker year.
- **Year-end "Tax Wrapped" recap** (free). Spotify-Wrapped-style annual summary: total earned, miles driven, top platform, busiest month, tax saved via deductions. Reuses entirely existing data; zero new infrastructure. Strong organic-sharing driver timed to filing season. Per [ROADMAP §9.5](ROADMAP.md).
- **Affiliate integration with tax filing software.** TurboTax, FreeTaxUSA, Cash App Taxes — gig workers in this app are warm, pre-qualified leads at exactly the right moment. Optional CPA referral directory for users who want a human. Per [ROADMAP §5](ROADMAP.md).

**Ships to:** general public, timed for filing-season launch.

</details>

## ⬜ v1.6 — Money-Moves & Pro Tools

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** turn the app's data into concrete financial decisions — the "amazing premium tier" features.

*(W-4 withholding optimizer was pulled forward to v1.1 since the W2 rebuild just shipped and it's the natural next step from the same infrastructure.)*

- **Self-employed health insurance deduction + SEP-IRA/Solo 401k contribution deductions.** Reduces estimated taxable income for gig workers who already pay SE health premiums or contribute to a retirement account. Per [ROADMAP §8.1](ROADMAP.md). Natural companion to v2.3's retirement nudges ("here's what you could save" → "here's how to actually open the account").
- **QuickBooks Self-Employed-compatible export.** Many self-employed users are already in this ecosystem; a drop-in import is a concrete premium hook beyond generic CSV.
- **CPA/tax-pro shareable summary package.** A dedicated "share with your preparer" flow — a structured package the CPA can work from directly, beyond the existing plain PDF. Per [ROADMAP §9.2](ROADMAP.md).
- **Multi-vehicle selection / tracking.** Per-entry vehicle selection, enabling the v2.3 vehicle break-even analysis per vehicle rather than averaging across the fleet.

**Ships to:** general public, premium. **Exit criteria:** each feature independently demonstrates a concrete dollar-value or time-saved benefit a user can point to. Only depends on v1.1 premium infra — can ship in parallel with v1.3/v1.4 if those run long.

</details>

## ⬜ v2.0 — AI Layer Begins (Premium+/AI Tier Launch)

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** first AI tier features ship, with a cost model validated before wide rollout.
- **AI tier cost/usage-cap model validated against real inference costs** ([ROADMAP §8.4](ROADMAP.md)) — gated on this, not the calendar; this is the one version where "feature complete" should yield to "financially sound"
- Natural language entry ("spent $40 on gas, made $180 on Flex today" → structured entry)
- AI deduction finder (pattern-based suggestions on expense/mileage data)
**Ships to:** general public, new Premium+/AI tier. **Exit criteria:** AI tier pricing covers per-user inference cost with margin at expected usage patterns.

</details>

## ⬜ v2.1 — Conversational Assistant

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
- Personalized tax optimization chat (RAG over user's own transaction history)
- Anomaly detection (flag weeks where earnings/mileage look off vs. historical pattern)
**Ships to:** AI tier.

</details>

## ⬜ v2.2 — Predictive Finance

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
- Predictive cash-flow ("you'll owe ~$X this quarter, adjust your weekly set-aside")
- **Bank/Plaid auto-sync.** High compliance overhead; legal review required before this ships per [ROADMAP §6](ROADMAP.md). **A SOC 2 roadmap (or equivalent compliance attestation) must be underway *before* this item starts** — this is when the app transitions from purely local-first to holding real bank-linked data, a materially different risk/compliance posture. Don't discover this as a blocker once v2.2 is already in progress.
- Promised-vs-actual pay discrepancy detection
**Ships to:** AI tier.

</details>

## ⬜ v2.3 — Financial Wellness Suite

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
- Envelope-style virtual buckets (tax/savings/spending)
- Vehicle break-even analysis (true cost per mile vs. standard mileage deduction, per vehicle using v1.6's multi-vehicle data)
- Retirement nudges with actual action (SEP-IRA/Roth partnership integration — connect the deduction math from v1.6 to actually opening an account)
**Ships to:** AI tier, with envelope buckets possibly soft-launched to premium as a teaser.

</details>

## ⬜ v3.0 — Platform Expansion

<details>
<summary>Status: Not Started</summary>

**Status: Not Started**
**Goal:** broaden reach once core product/monetization is proven.
- **Apple Watch companion app.** Glanceable today's earnings and set-aside on a wrist — genuine safety/convenience win for drivers who shouldn't touch their phone, not just a novelty. Per [ROADMAP §9.3](ROADMAP.md).
- **Multi-language support.** A large share of gig drivers are not native English speakers — genuinely underserved, a real differentiator. Prioritize Spanish first given gig-worker demographics. Per [ROADMAP §9.3](ROADMAP.md).
- **Web dashboard** — revisit whether there's a case to pull this forward earlier as a free-tier lead-magnet tax calculator (the tax engine is already a standalone package) or a "manage from desktop" companion.
- **B2B / white-label exploration** — gig platforms embedding tax tooling for their own drivers. Per [ROADMAP §5](ROADMAP.md).

</details>

## Sequencing notes

<details>
<summary>v0.1–v0.3 are not public — they existed to de-risk the tax engine and core loop before an…</summary>

- **v0.1–v0.3 are not public** — they existed to de-risk the tax engine and core loop before any store submission or marketing spend.
- **Wire up Sentry and analytics immediately post–App Store approval.** Both are scaffolded with no real backend. You need crash data and usage patterns before making confident v1.1 feature-prioritization decisions — don't start building v1.1 blind to what's actually happening in production.
- **Android CI and Play Console setup can start in parallel with v1.1 development.** The RevenueCat product/entitlement config needs to exist before Play Billing can go live, but the CI pipeline and store listing work are independent of that.
- **Don't start v1.4 (Platform Auto-Sync) before v1.1 (payment infra) is stable** — auto-sync is the single best premium conversion driver and shouldn't debut with a broken checkout flow.
- **v1.3 (GPS mileage) can ship before v1.4 (platform auto-sync)** — no backend dependency, higher per-user daily retention value. IRS-compliant mileage log fields in v1.1 provide the data-model foundation so no schema rework is needed.
- **v1.5 (filing season toolkit) is date-sensitive** — target early January regardless of where v1.3/v1.4 stand. The 1099 reconciliation and affiliate integration have a narrow value window (Jan–April).
- **v1.6 (Money-Moves) only depends on v1.1's premium infra** — it doesn't require platform auto-sync or GPS mileage, so it can run in parallel with v1.3/v1.4 if those take longer than expected.
- **v2.0 is gated on the cost model, not the calendar** — don't ship AI tier features until per-user inference economics are validated with margin.
- **SOC 2 roadmap must be underway before v2.2 (Bank/Plaid) starts** — that's when the app transitions from purely local-first to holding bank-linked data, a materially different compliance posture than everything built before it.

</details>
